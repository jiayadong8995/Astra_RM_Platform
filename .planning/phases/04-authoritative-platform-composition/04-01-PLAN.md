---
phase: 04-authoritative-platform-composition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - robot_platform/runtime/generated/stm32h7_ctrl_board_raw/Src/main.c
  - robot_platform/runtime/generated/stm32h7_ctrl_board_raw/Src/freertos.c
  - robot_platform/runtime/bsp/sitl/main_sitl.c
  - robot_platform/runtime/app/balance_chassis/app_startup/balance_chassis_app_startup.h
  - robot_platform/runtime/app/balance_chassis/app_startup/balance_chassis_app_startup.c
  - robot_platform/runtime/app/balance_chassis/app_bringup/task_registry.c
  - robot_platform/runtime/app/balance_chassis/app_bringup/task_registry.h
  - robot_platform/runtime/app/balance_chassis/app_bringup/freertos_app.c
  - robot_platform/runtime/tests/host/test_balance_app_startup.c
  - robot_platform/CMakeLists.txt
autonomous: true
requirements:
  - ARCH-01
  - ARCH-03
must_haves:
  truths:
    - "Hardware startup follows the explicit generated path `main.c -> MX_FREERTOS_Init() -> scheduler` instead of idling in a bare while-loop."
    - "Hardware and SITL both call one shared `balance_chassis` app-startup API for project business bring-up."
    - "Generated `freertos.c` is the only authoritative owner of `MX_FREERTOS_Init()`."
  artifacts:
    - path: robot_platform/runtime/app/balance_chassis/app_startup/balance_chassis_app_startup.h
      provides: "Authoritative shared app-startup contract."
    - path: robot_platform/runtime/generated/stm32h7_ctrl_board_raw/Src/freertos.c
      provides: "Generated RTOS host entry that delegates to the app-startup API."
    - path: robot_platform/runtime/tests/host/test_balance_app_startup.c
      provides: "Host regression for startup convergence and authority."
  key_links:
    - from: robot_platform/runtime/generated/stm32h7_ctrl_board_raw/Src/freertos.c
      to: robot_platform/runtime/app/balance_chassis/app_startup/balance_chassis_app_startup.c
      via: "shared startup call"
      pattern: "balance_chassis_app_startup\\("
    - from: robot_platform/runtime/bsp/sitl/main_sitl.c
      to: robot_platform/runtime/app/balance_chassis/app_startup/balance_chassis_app_startup.c
      via: "shared startup call"
      pattern: "balance_chassis_app_startup\\("
---

<objective>
Make startup authority real in code by introducing one shared app-startup API, wiring generated hardware startup and SITL to it, and removing project-owned parallel authority over `MX_FREERTOS_Init()`.

Purpose: Implement D-01 through D-07 and D-12 through D-15 directly in source so Phase 4 starts from real bring-up truth, not documentation-only intent.
Output: Shared app-startup contract and implementation, converged hardware/SITL startup wiring, and a host regression that proves startup authority.
</objective>

<execution_context>
@/home/xbd/.codex/get-shit-done/workflows/execute-plan.md
@/home/xbd/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-authoritative-platform-composition/04-CONTEXT.md
@.planning/phases/04-authoritative-platform-composition/04-RESEARCH.md
@.planning/phases/04-authoritative-platform-composition/04-VALIDATION.md
@.planning/phases/02-host-safety-control-verification/02-01-SUMMARY.md
@robot_platform/runtime/generated/stm32h7_ctrl_board_raw/Src/main.c
@robot_platform/runtime/generated/stm32h7_ctrl_board_raw/Src/freertos.c
@robot_platform/runtime/bsp/sitl/main_sitl.c
@robot_platform/runtime/app/balance_chassis/app_bringup/freertos_app.c
@robot_platform/runtime/app/balance_chassis/app_bringup/task_registry.c
@robot_platform/runtime/README.md
@robot_platform/CMakeLists.txt

<interfaces>
From `robot_platform/runtime/generated/stm32h7_ctrl_board_raw/Src/freertos.c`:
```c
void MX_FREERTOS_Init(void);
void StartDefaultTask(void const * argument);
```

From `robot_platform/runtime/app/balance_chassis/app_bringup/task_registry.h`:
```c
void balance_chassis_start_tasks(void);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the authoritative shared app-startup API and startup regression surface</name>
  <files>
robot_platform/runtime/app/balance_chassis/app_startup/balance_chassis_app_startup.h
robot_platform/runtime/app/balance_chassis/app_startup/balance_chassis_app_startup.c
robot_platform/runtime/app/balance_chassis/app_bringup/task_registry.c
robot_platform/runtime/app/balance_chassis/app_bringup/task_registry.h
robot_platform/runtime/tests/host/test_balance_app_startup.c
robot_platform/CMakeLists.txt
  </files>
  <read_first>
robot_platform/runtime/app/balance_chassis/app_bringup/task_registry.c
robot_platform/runtime/app/balance_chassis/app_bringup/task_registry.h
robot_platform/runtime/app/balance_chassis/app_bringup/freertos_app.c
robot_platform/runtime/generated/stm32h7_ctrl_board_raw/Src/freertos.c
robot_platform/runtime/bsp/sitl/main_sitl.c
robot_platform/CMakeLists.txt
.planning/phases/04-authoritative-platform-composition/04-CONTEXT.md
.planning/phases/04-authoritative-platform-composition/04-RESEARCH.md
.planning/phases/04-authoritative-platform-composition/04-VALIDATION.md
.planning/phases/02-host-safety-control-verification/02-01-SUMMARY.md
  </read_first>
  <action>Create `robot_platform/runtime/app/balance_chassis/app_startup/balance_chassis_app_startup.h` and `.c` with one public function `void balance_chassis_app_startup(void);` that owns only project task registration and project-level runtime wiring per D-03, D-05, and D-06. Keep OS startup, scheduler start, and board init out of this API. Reuse `balance_chassis_start_tasks()` as the first implementation body so the new seam is thin and authoritative rather than inventing a second orchestration layer. Add `robot_platform/runtime/tests/host/test_balance_app_startup.c` and wire it into `robot_platform/CMakeLists.txt` as `test_balance_app_startup`; the test should prove the new API exists, links cleanly, and reaches the task-registration seam without depending on a project-owned `MX_FREERTOS_Init()`. Refresh the host-test build path in this task so later plans can depend on a real `build/robot_platform_host_tests` target tree instead of a placeholder Wave 0 note.</action>
  <acceptance_criteria>
    - `robot_platform/runtime/app/balance_chassis/app_startup/balance_chassis_app_startup.h` contains the exact declaration `void balance_chassis_app_startup(void);`.
    - `robot_platform/runtime/app/balance_chassis/app_startup/balance_chassis_app_startup.c` contains the exact call `balance_chassis_start_tasks();`.
    - `robot_platform/runtime/tests/host/test_balance_app_startup.c` exists and contains the exact string `balance_chassis_app_startup`.
    - `robot_platform/CMakeLists.txt` contains the exact target name `test_balance_app_startup`.
    - `rg -n "balance_chassis_app_startup|test_balance_app_startup" robot_platform/runtime/app/balance_chassis/app_startup robot_platform/runtime/tests/host/test_balance_app_startup.c robot_platform/CMakeLists.txt` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>cmake -S robot_platform -B build/robot_platform_host_tests -G "Unix Makefiles" -DPLATFORM_TARGET_HW=OFF -DPLATFORM_TARGET_SIM=OFF -DPLATFORM_HOST_TESTS=ON -DPLATFORM_HOST_TEST_SANITIZERS=ON && cmake --build build/robot_platform_host_tests --target test_balance_app_startup -j4 && ctest --test-dir build/robot_platform_host_tests --output-on-failure -R test_balance_app_startup</automated>
  </verify>
  <done>The repository exposes one shared `balance_chassis_app_startup()` contract and a real host regression can build and execute against it.</done>
</task>

<task type="auto">
  <name>Task 2: Converge hardware and SITL startup onto the shared app-startup API</name>
  <files>
robot_platform/runtime/generated/stm32h7_ctrl_board_raw/Src/main.c
robot_platform/runtime/generated/stm32h7_ctrl_board_raw/Src/freertos.c
robot_platform/runtime/bsp/sitl/main_sitl.c
robot_platform/runtime/app/balance_chassis/app_bringup/freertos_app.c
robot_platform/CMakeLists.txt
  </files>
  <read_first>
robot_platform/runtime/generated/stm32h7_ctrl_board_raw/Src/main.c
robot_platform/runtime/generated/stm32h7_ctrl_board_raw/Src/freertos.c
robot_platform/runtime/bsp/sitl/main_sitl.c
robot_platform/runtime/app/balance_chassis/app_bringup/freertos_app.c
robot_platform/runtime/app/balance_chassis/app_startup/balance_chassis_app_startup.h
robot_platform/runtime/app/balance_chassis/app_startup/balance_chassis_app_startup.c
robot_platform/CMakeLists.txt
.planning/phases/04-authoritative-platform-composition/04-CONTEXT.md
.planning/phases/04-authoritative-platform-composition/04-RESEARCH.md
  </read_first>
  <action>Make the source tree express D-01 through D-04 literally. In generated `main.c`, insert the RTOS handoff inside user-code blocks so hardware follows `main.c -> MX_FREERTOS_Init() -> scheduler` instead of falling into the idle while-loop; use the generated CMSIS/FreeRTOS startup pattern already compatible with `freertos.c`. In generated `freertos.c`, include `balance_chassis_app_startup.h` and call `balance_chassis_app_startup();` from `MX_FREERTOS_Init()` while leaving generated ownership of the function intact per D-02 and D-07. In `main_sitl.c`, stop using `freertos_app.c` as the business-start owner: call `balance_chassis_app_startup();` directly before `vTaskStartScheduler();`, keep SITL host entry separate, and preserve the log surface. In `freertos_app.c`, remove the exported `MX_FREERTOS_Init()` symbol entirely; either delete the file from target composition or reduce it to a clearly non-authoritative compatibility wrapper that does not own startup. Update `robot_platform/CMakeLists.txt` so SITL no longer compiles a second startup owner for business bring-up.</action>
  <acceptance_criteria>
    - `robot_platform/runtime/generated/stm32h7_ctrl_board_raw/Src/main.c` contains the exact string `MX_FREERTOS_Init();`.
    - `robot_platform/runtime/generated/stm32h7_ctrl_board_raw/Src/freertos.c` contains the exact string `balance_chassis_app_startup();`.
    - `robot_platform/runtime/bsp/sitl/main_sitl.c` contains the exact string `balance_chassis_app_startup();`.
    - `robot_platform/runtime/app/balance_chassis/app_bringup/freertos_app.c` does not contain the string `void MX_FREERTOS_Init`.
    - `rg -n "balance_chassis_app_startup\\(|MX_FREERTOS_Init\\(" robot_platform/runtime/generated/stm32h7_ctrl_board_raw/Src/main.c robot_platform/runtime/generated/stm32h7_ctrl_board_raw/Src/freertos.c robot_platform/runtime/bsp/sitl/main_sitl.c` returns matches for all three files.
  </acceptance_criteria>
  <verify>
    <automated>cmake -S robot_platform -B build/robot_platform_host_tests -G "Unix Makefiles" -DPLATFORM_TARGET_HW=OFF -DPLATFORM_TARGET_SIM=OFF -DPLATFORM_HOST_TESTS=ON -DPLATFORM_HOST_TEST_SANITIZERS=ON && cmake --build build/robot_platform_host_tests --target test_balance_app_startup -j4 && ctest --test-dir build/robot_platform_host_tests --output-on-failure -R test_balance_app_startup</automated>
  </verify>
  <done>Generated hardware startup and SITL both reuse `balance_chassis_app_startup()`, and only generated `freertos.c` remains authoritative for `MX_FREERTOS_Init()`.</done>
</task>

</tasks>

<verification>
Refresh `build/robot_platform_host_tests` first, because `04-VALIDATION.md` marks the host-test tree as a Wave 0 dependency. Use the startup regression as the gate before any ownership migration in 04-02.
</verification>

<success_criteria>
There is one shared `balance_chassis_app_startup()` seam, generated hardware startup owns `MX_FREERTOS_Init()`, SITL uses the same business-start API, and a host regression proves the convergence.
</success_criteria>

<output>
After completion, create `.planning/phases/04-authoritative-platform-composition/04-01-SUMMARY.md`
</output>
