---
phase: v2-02-seam-migration
plan: 02
type: execute
wave: 2
depends_on: ["v2-02-01"]
files_modified:
  - robot_platform/runtime/control/execution/actuator_gateway.h
  - robot_platform/runtime/control/execution/actuator_gateway.c
  - robot_platform/runtime/control/state/ins_task.c
  - robot_platform/runtime/app/balance_chassis/app_bringup/remote_task.c
  - robot_platform/runtime/tests/host/test_actuator_gateway.c
  - robot_platform/runtime/tests/host/test_support/device_layer_stubs.c
  - robot_platform/runtime/tests/host/test_support/device_layer_stubs.h
  - robot_platform/runtime/bsp/ports_fake.c
  - robot_platform/CMakeLists.txt
autonomous: true

must_haves:
  truths:
    - "ins_task calls platform_imu_read instead of platform_device_read_default_imu"
    - "remote_task calls platform_remote_read instead of platform_device_read_default_remote"
    - "actuator_gateway calls platform_motor_read_feedback and platform_motor_write_command instead of device_layer functions"
    - "test_actuator_gateway injects through ports_fake instead of device_layer_stubs"
    - "All 12 CTest targets pass"
    - "test_balance_safety_path stays green after task migration"
    - "ports_fake hooks now intercept natively without device_layer bridge"
  artifacts:
    - path: "robot_platform/runtime/control/execution/actuator_gateway.h"
      provides: "Gateway header including ports.h instead of device_layer.h"
      contains: "ports.h"
    - path: "robot_platform/runtime/control/execution/actuator_gateway.c"
      provides: "Gateway calling BSP port functions"
      contains: "platform_motor_write_command"
    - path: "robot_platform/runtime/control/state/ins_task.c"
      provides: "INS task calling BSP port for IMU"
      contains: "platform_imu_read"
    - path: "robot_platform/runtime/app/balance_chassis/app_bringup/remote_task.c"
      provides: "Remote task calling BSP port for remote"
      contains: "platform_remote_read"
  key_links:
    - from: "robot_platform/runtime/control/execution/actuator_gateway.c"
      to: "robot_platform/runtime/bsp/ports.h"
      via: "include and function calls"
      pattern: "platform_motor_write_command|platform_motor_read_feedback"
    - from: "robot_platform/runtime/control/state/ins_task.c"
      to: "robot_platform/runtime/bsp/ports.h"
      via: "include and function call"
      pattern: "platform_imu_read"
    - from: "robot_platform/runtime/app/balance_chassis/app_bringup/remote_task.c"
      to: "robot_platform/runtime/bsp/ports.h"
      via: "include and function call"
      pattern: "platform_remote_read"
    - from: "robot_platform/runtime/tests/host/test_actuator_gateway.c"
      to: "robot_platform/runtime/bsp/ports_fake.h"
      via: "include and hook setup"
      pattern: "platform_ports_fake"
---

<objective>
Migrate the 3 control tasks (ins_task, remote_task, actuator_gateway) from device_layer calls to BSP port calls, and migrate test_actuator_gateway from device_layer_stubs to ports_fake injection.

Purpose: After Plan 01 migrated the test injection API, this plan migrates the actual runtime call sites. Once complete, no control task calls device_layer functions, and the ports_fake hooks intercept natively (the device_layer bridge from Plan 01 becomes unnecessary and is removed).

Output: 3 migrated task files, migrated test_actuator_gateway, cleaned up ports_fake.c (bridge removed), updated CMakeLists.txt.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v2-02-seam-migration/v2-02-01-SUMMARY.md
@robot_platform/runtime/bsp/ports.h
@robot_platform/runtime/bsp/ports_fake.h
@robot_platform/runtime/bsp/ports_fake.c
@robot_platform/runtime/control/execution/actuator_gateway.h
@robot_platform/runtime/control/execution/actuator_gateway.c
@robot_platform/runtime/control/state/ins_task.c
@robot_platform/runtime/app/balance_chassis/app_bringup/remote_task.c
@robot_platform/runtime/tests/host/test_actuator_gateway.c
@robot_platform/runtime/tests/host/test_support/device_layer_stubs.c
@robot_platform/runtime/tests/host/test_support/device_layer_stubs.h
@robot_platform/CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate control tasks from device_layer to BSP port calls</name>
  <files>
    robot_platform/runtime/control/execution/actuator_gateway.h
    robot_platform/runtime/control/execution/actuator_gateway.c
    robot_platform/runtime/control/state/ins_task.c
    robot_platform/runtime/app/balance_chassis/app_bringup/remote_task.c
  </files>
  <action>
CRITICAL: Migrate one file at a time and run tests after each. test_balance_safety_path must stay green at every step.

**Step 1: Migrate ins_task.c**
- Replace `#include "../../device/device_layer.h"` with `#include "../../bsp/ports.h"` (relative path from control/state/ to bsp/)
- Replace `platform_device_read_default_imu(&sample)` with `platform_imu_read(&sample)`
- Run tests — verify test_balance_safety_path passes

**Step 2: Migrate remote_task.c**
- Replace `#include "../../../device/device_layer.h"` with `#include "../../../bsp/ports.h"` (relative path from app/balance_chassis/app_bringup/ to bsp/)
- Replace `platform_device_read_default_remote(&runtime->rc_input)` with `platform_remote_read(&runtime->rc_input)`
- Run tests — verify test_balance_safety_path passes

**Step 3: Migrate actuator_gateway.h and actuator_gateway.c**
- In `actuator_gateway.h`: replace `#include "../../device/device_layer.h"` with `#include "../../bsp/ports.h"`
- In `actuator_gateway.c`:
  - `platform_actuator_gateway_init()`: currently calls `platform_device_init_default_profile()`. The BSP port interface has no init function — the port implementations are stateless or self-initializing. Replace the body with a no-op (empty function body, or just `(void)0;`). The function signature stays for API compatibility.
  - `platform_actuator_gateway_capture_feedback()`: replace `platform_device_read_default_feedback(feedback_msg)` with `platform_motor_read_feedback(feedback_msg)`
  - `platform_actuator_gateway_dispatch_command()`: replace `platform_device_write_default_command(&device_command)` with `platform_motor_write_command(&device_command.motors)`. Note: the port function takes `platform_motor_command_set_t *` (the `.motors` field), not the full `platform_device_command_t`.
- Run tests — verify test_balance_safety_path passes

After all 3 steps, the ports_fake hooks now intercept natively for read_imu, read_remote, and read_feedback (the tasks call port functions directly, which go to ports_fake.c in test builds). The write_command path also goes through ports_fake natively now.
  </action>
  <verify>
Run: `cd /home/xbd/workspace/codes/Astra_RM_Platform && python3 -m robot_platform.tools.platform_cli.main test`
All 12 CTest targets pass.
Run: `grep -rn 'platform_device_read_default_\|platform_device_write_default_\|platform_device_init_default_profile' robot_platform/runtime/control/ robot_platform/runtime/app/`
Returns zero matches in control task code (only device_layer.c itself may still contain these).
  </verify>
  <done>
ins_task, remote_task, and actuator_gateway call BSP port functions. No control task references device_layer functions. test_balance_safety_path green at every sub-step.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate test_actuator_gateway to ports_fake and remove device_layer bridge</name>
  <files>
    robot_platform/runtime/tests/host/test_actuator_gateway.c
    robot_platform/runtime/tests/host/test_support/device_layer_stubs.c
    robot_platform/runtime/tests/host/test_support/device_layer_stubs.h
    robot_platform/runtime/bsp/ports_fake.c
    robot_platform/CMakeLists.txt
  </files>
  <action>
**Step 1: Migrate test_actuator_gateway.c**

`test_actuator_gateway` currently links `device_layer_stubs.c` which provides link-time stubs for `platform_device_init_default_profile`, `platform_device_read_default_feedback`, `platform_device_write_default_command`. After Task 1, actuator_gateway.c calls port functions instead, so the stubs need to become port-level fakes.

Rewrite `test_actuator_gateway.c`:
- Replace `#include "device_layer_stubs.h"` with `#include "ports_fake.h"`
- The test currently uses:
  - `platform_test_device_layer_stubs_reset()` — replace with `platform_ports_fake_reset_hooks()`
  - `platform_test_device_layer_get_init_call_count()` — `platform_actuator_gateway_init()` is now a no-op, so `test_init_calls_default_profile_once` should assert the function completes without error (remove the init_call_count assertion, or just verify write_call_count is 0)
  - `platform_test_device_layer_seed_feedback()` — replace with setting up a ports_fake feedback hook that returns the seeded data
  - `platform_test_device_layer_get_write_call_count()` — replace with a counter in the write_command hook context
  - `platform_test_device_layer_get_last_command()` — replace with capturing the command in the write_command hook context

Create a local test context struct in test_actuator_gateway.c:
```c
typedef struct {
    platform_device_feedback_t seeded_feedback;
    platform_device_result_t feedback_result;
    platform_motor_command_set_t last_command;
    uint32_t write_count;
    uint32_t init_count;
} test_actuator_gw_context_t;
```

Create local hook functions:
- `static platform_device_result_t fake_read_feedback(platform_device_feedback_t *fb, void *ctx)` — copies seeded_feedback, returns feedback_result
- `static platform_device_result_t fake_write_command(const platform_motor_command_set_t *cmd, void *ctx)` — captures last_command, increments write_count

Update test functions:
- `test_init_calls_default_profile_once`: gateway_init is now no-op. Simplify to just call it and verify no crash. Or remove this test if it adds no value. Better: keep it, just assert `platform_actuator_gateway_init()` returns without side effects (write_count stays 0).
- `test_capture_feedback_forwards_result_and_payload`: set up feedback hook, call capture_feedback, assert result and payload match.
- `test_dispatch_maps_contract_motors_into_device_command`: set up write_command hook, call dispatch, assert captured `last_command` (now `platform_motor_command_set_t`) matches expected. Adjust assertions: old code compared `written_command->motors.joints[...]` — now compare `last_command.joints[...]` directly.
- `test_dispatch_clears_validity_when_actuator_or_control_is_disabled`: same adjustment.

Update CMakeLists.txt for `test_actuator_gateway` target:
- Replace `${PLATFORM_HOST_TEST_DIR}/test_support/device_layer_stubs.c` with `${PLATFORM_RUNTIME_DIR}/bsp/ports_fake.c`
- Replace `${PLATFORM_RUNTIME_DIR}/device` include dir with `${PLATFORM_RUNTIME_DIR}/bsp` in target_include_directories
- Keep `${PLATFORM_CONTROL_CONTRACT_DIR}` include (needed for types)

**Step 2: Remove device_layer bridge from ports_fake.c**

After Task 1 migrated the tasks and this step migrated test_actuator_gateway, the device_layer bridge in ports_fake.c (the internal `platform_device_set_test_hooks` call) is no longer needed. The port functions are called directly by the tasks, and ports_fake hooks intercept them natively.

Remove from ports_fake.c:
- The `#include "device_layer.h"` (if added in Plan 01 for the bridge)
- The adapter functions that wrapped ports_fake hooks into device_layer hooks
- The `platform_device_set_test_hooks` / `platform_device_reset_test_hooks` calls inside `platform_ports_fake_set_hooks` / `platform_ports_fake_reset_hooks`

ports_fake.c should now be clean: just the hook storage, set/reset functions, and the 4 port function implementations that delegate to hooks or return zeroed OK.

**Step 3: Verify device_layer_stubs are no longer needed**

After migration, `device_layer_stubs.c` and `device_layer_stubs.h` should have no consumers. Verify:
- `grep -r 'device_layer_stubs' robot_platform/` — should only match the files themselves and possibly CMakeLists.txt (which we already updated)
- If no consumers remain, leave the files in place for now (Phase 3 will clean up all device_layer artifacts). Do NOT delete them in this plan to minimize risk.

Run all tests.
  </action>
  <verify>
Run: `cd /home/xbd/workspace/codes/Astra_RM_Platform && python3 -m robot_platform.tools.platform_cli.main test`
All 12 CTest targets pass.
Run: `grep -rn 'device_layer_stubs' robot_platform/CMakeLists.txt`
Returns zero matches (stubs no longer linked by any target).
Run: `grep -rn '#include "device_layer.h"' robot_platform/runtime/bsp/ports_fake.c`
Returns zero matches (bridge removed).
Run: `grep -rn 'platform_device_set_test_hooks\|platform_device_reset_test_hooks' robot_platform/runtime/bsp/ports_fake.c`
Returns zero matches (bridge removed).
  </verify>
  <done>
test_actuator_gateway uses ports_fake injection. device_layer_stubs no longer linked by any CMake target. ports_fake.c is clean — no device_layer bridge. All 12 CTest targets pass.
  </done>
</task>

</tasks>

<verification>
1. `python3 -m robot_platform.tools.platform_cli.main test` — all 12 CTest targets pass
2. `grep -rn 'platform_device_read_default_\|platform_device_write_default_\|platform_device_init_default_profile' robot_platform/runtime/control/ robot_platform/runtime/app/` — zero matches (no control task calls device_layer)
3. `grep -rn '#include "device_layer.h"' robot_platform/runtime/control/ robot_platform/runtime/app/` — zero matches (no control code includes device_layer)
4. `grep -rn 'platform_device_set_test_hooks\|platform_device_reset_test_hooks' robot_platform/runtime/tests/ robot_platform/runtime/bsp/` — zero matches (no test or fake uses old hook API)
5. test_balance_safety_path specifically passes
6. All 6 safety oracles (SAFE-01..06) pass — verified by test_safety_mapping, test_safety_arming, test_safety_saturation, test_safety_sensor_faults, test_safety_wheel_leg, test_balance_safety_path all passing
</verification>

<success_criteria>
- ins_task, remote_task, actuator_gateway call BSP port functions exclusively
- test_actuator_gateway injects through ports_fake, not device_layer_stubs
- ports_fake.c has no device_layer bridge — hooks intercept natively
- No control task or test references device_layer functions
- All 12 CTest targets pass with zero regressions
- test_balance_safety_path green throughout migration
</success_criteria>

<output>
After completion, create `.planning/phases/v2-02-seam-migration/v2-02-02-SUMMARY.md`
</output>
