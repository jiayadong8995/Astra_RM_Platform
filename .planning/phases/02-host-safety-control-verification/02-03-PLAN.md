---
phase: 02-host-safety-control-verification
plan: "03"
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - robot_platform/runtime/app/balance_chassis/app_bringup/remote_task.c
  - robot_platform/runtime/app/balance_chassis/app_bringup/remote_task.h
  - robot_platform/runtime/device/device_layer.c
  - robot_platform/runtime/device/device_layer.h
  - robot_platform/runtime/device/device_profile_sitl.c
  - robot_platform/runtime/app/balance_chassis/app_intent/remote_intent_state.h
  - robot_platform/runtime/tests/host/test_balance_safety_path.c
  - robot_platform/runtime/tests/host/test_safety_wheel_leg.c
  - robot_platform/tools/platform_cli/main.py
  - robot_platform/tools/platform_cli/tests/test_main.py
autonomous: true
requirements:
  - HOST-02
  - HOST-03
  - SAFE-05
  - SAFE-06
must_haves:
  truths:
    - Lost or stale command input transitions the current path to a defined safe behavior and fails verification if enabled output persists.
    - The Phase 2 artifact can distinguish stale-command command-path failures from other case failures without depending on Phase 3 fake-link work.
    - The regression suite includes narrow wheel-leg danger signatures that fail if unsafe combined leg and wheel output patterns remain enabled.
  artifacts:
    - path: robot_platform/runtime/tests/host/test_safety_wheel_leg.c
      provides: SAFE-06 narrow wheel-leg danger-signature regression coverage.
    - path: robot_platform/runtime/tests/host/test_balance_safety_path.c
      provides: Current-path stale-command regression coverage on the actual remote ingress chain.
    - path: robot_platform/tools/platform_cli/main.py
      provides: Final `verify phase2` case matrix and verdict artifact output.
  key_links:
    - from: robot_platform/runtime/app/balance_chassis/app_bringup/remote_task.c
      to: robot_platform/runtime/tests/host/test_balance_safety_path.c
      via: stale-remote or stale-command fault injection on the actual ingress path
      pattern: platform_device_read_default_remote
    - from: robot_platform/runtime/tests/host/test_safety_wheel_leg.c
      to: robot_platform/runtime/control/contracts/actuator_command.h
      via: explicit enabled wheel-leg danger signature assertions
      pattern: left_leg_joint
---

<objective>
Finish Phase 2 by proving stale-command safe behavior and narrow wheel-leg risk signatures on the current path, then package the full case matrix into the authoritative Phase 2 artifact.

Purpose: Close SAFE-05 and SAFE-06 without drifting into Phase 3 fake-link proof scope.
Output: Stale-command current-path regressions, wheel-leg danger-signature regressions, and the completed `verify phase2` verdict matrix without Phase 3 report-stack drift.
</objective>

<execution_context>
@/home/xbd/.codex/get-shit-done/workflows/execute-plan.md
@/home/xbd/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-host-safety-control-verification/02-CONTEXT.md
@.planning/phases/02-host-safety-control-verification/02-RESEARCH.md
@.planning/phases/02-host-safety-control-verification/02-01-PLAN.md
@.planning/phases/02-host-safety-control-verification/02-02-PLAN.md
@robot_platform/runtime/app/balance_chassis/app_bringup/remote_task.c
@robot_platform/runtime/device/device_layer.c
@robot_platform/runtime/device/device_profile_sitl.c
@robot_platform/runtime/app/balance_chassis/app_intent/remote_intent_state.h
@robot_platform/tools/platform_cli/main.py

<interfaces>
Current remote ingress that must own stale-command freshness for the real path:
```c
(void)platform_device_read_default_remote(&rc_input);
```

Phase 2 should continue judging these output flags and payloads:
```c
typedef struct {
  bool start;
  bool control_enable;
  bool actuator_enable;
  ...
} platform_actuator_command_t;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add stale-command and link-ingress safety cases on the current path</name>
  <read_first>
    - robot_platform/runtime/app/balance_chassis/app_bringup/remote_task.c
    - robot_platform/runtime/device/device_layer.c
    - robot_platform/runtime/device/device_profile_sitl.c
    - robot_platform/runtime/app/balance_chassis/app_intent/remote_intent_state.h
    - robot_platform/runtime/tests/host/test_balance_safety_path.c
    - robot_platform/tools/platform_cli/main.py
  </read_first>
  <files>robot_platform/runtime/app/balance_chassis/app_bringup/remote_task.c, robot_platform/runtime/app/balance_chassis/app_bringup/remote_task.h, robot_platform/runtime/device/device_layer.c, robot_platform/runtime/device/device_layer.h, robot_platform/runtime/device/device_profile_sitl.c, robot_platform/runtime/app/balance_chassis/app_intent/remote_intent_state.h, robot_platform/runtime/tests/host/test_balance_safety_path.c, robot_platform/tools/platform_cli/main.py, robot_platform/tools/platform_cli/tests/test_main.py</files>
  <behavior>
    - Test 1: SAFE-05 fails when stale or missing command input leaves `control_enable` or `actuator_enable` asserted.
    - Test 2: `verify phase2 --case stale_command` runs the authoritative current-path case and records a specific command-loss failure reason when the safe behavior is violated.
    - Test 3: Command-ingress failures are labeled as command-path faults in the JSON artifact without claiming full Phase 3 communication/fake-link coverage.
  </behavior>
  <action>Implement stale-command authorship and judgment on the actual command ingress path used by `remote_task`, not on `remote_topics.c`. Use the device/profile seam to supply remote samples with explicit freshness or sequencing metadata, or add the minimum freshness tracking needed around the published `robot_intent` boundary, but the host oracle must prove that expired command input on the real chain drops or degrades enabled output per D-08 and D-17. Extend `verify phase2` to run these cases and emit explicit failure reasons such as stale command, missing command, or unsafe enabled output; keep the Phase 2 artifact local to `verify phase2` and its direct tests instead of routing through Phase 3 sim/fake-link reporting surfaces.</action>
  <verify>
    <automated>python3 -m robot_platform.tools.platform_cli.main verify phase2 --project balance_chassis --case stale_command --report build/verification_reports/phase2_stale_command.json</automated>
  </verify>
  <acceptance_criteria>
    - Stale-command freshness is authored and judged on the actual command ingress path used by `remote_task`.
    - Stale-command behavior is exercised on the current path, not by a direct controller bypass.
    - Failure reasons distinguish command ingress faults from sensor or mapping faults.
    - The implementation adds only the minimum state needed for deterministic stale-command judgment.
  </acceptance_criteria>
  <done>SAFE-05 is enforced by an authoritative current-path stale-command case and surfaced through `verify phase2`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encode narrow wheel-leg danger signatures and finish the Phase 2 verdict matrix</name>
  <read_first>
    - robot_platform/runtime/tests/host/test_balance_safety_path.c
    - robot_platform/tools/platform_cli/main.py
    - .planning/phases/02-host-safety-control-verification/02-RESEARCH.md
  </read_first>
  <files>robot_platform/runtime/tests/host/test_safety_wheel_leg.c, robot_platform/tools/platform_cli/main.py, robot_platform/tools/platform_cli/tests/test_main.py</files>
  <behavior>
    - Test 1: SAFE-06 fails when one or two explicit wheel-leg danger signatures produce unsafe combined enabled wheel and leg output patterns on the current path.
    - Test 2: The final `verify phase2` artifact includes these wheel-leg cases with explicit verdict fields and does not collapse them into vague “stability” language.
    - Test 3: CLI report tests remain green while Phase 2 artifact shaping stays local to `verify phase2`.
  </behavior>
  <action>Encode one or two narrow wheel-leg danger signatures tied to the current `balance_chassis` path per D-19 through D-21. Keep the signatures concrete, for example unsafe simultaneous wheel current and leg torque enable under bad support/attitude conditions, and judge them from `actuator_command` plus enable bits. Finish the Phase 2 case matrix in `verify phase2` and update only the direct CLI/report tests needed for stable machine-readable verdicts. Do not turn this into a broad stability proof, replay system, or Phase 3 communication diagnostic surface.</action>
  <verify>
    <automated>python3 -m unittest robot_platform.tools.platform_cli.tests.test_main -v && ctest --test-dir build/robot_platform_host_tests --output-on-failure -R test_safety_wheel_leg</automated>
  </verify>
  <acceptance_criteria>
    - Wheel-leg regressions stay narrow, explicit, and tied to observable unsafe patterns.
    - The Phase 2 JSON artifact reports these cases with stable machine-readable verdict fields.
    - No broader fake-link, sim-runner, or architecture-cleanup scope is introduced.
  </acceptance_criteria>
  <done>SAFE-06 is covered by explicit wheel-leg danger signatures and the final Phase 2 artifact reports the full safety-verdict matrix.</done>
</task>

</tasks>

<verification>
Run `python3 -m robot_platform.tools.platform_cli.main verify phase2 --project balance_chassis --report build/verification_reports/phase2_balance_chassis.json`, `python3 -m unittest robot_platform.tools.platform_cli.tests.test_main -v`, and `ctest --test-dir build/robot_platform_host_tests --output-on-failure -R test_safety_wheel_leg`. A green result proves stale-command and wheel-leg risk coverage is part of the authoritative Phase 2 artifact.
</verification>

<success_criteria>
Developers can run one authoritative Phase 2 command that exercises stale-command and wheel-leg danger-signature cases on the live current path and emits machine-readable safety verdicts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-host-safety-control-verification/02-03-SUMMARY.md`
</output>
