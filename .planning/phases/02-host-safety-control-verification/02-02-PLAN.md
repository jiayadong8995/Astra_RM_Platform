---
phase: 02-host-safety-control-verification
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - robot_platform/runtime/control/controllers/balance_controller.c
  - robot_platform/runtime/control/state/ins_state_estimator.c
  - robot_platform/runtime/control/execution/actuator_gateway.c
  - robot_platform/runtime/control/constraints/actuator_constraints.c
  - robot_platform/runtime/app/balance_chassis/app_intent/remote_intent.c
  - robot_platform/runtime/tests/host/test_safety_mapping.c
  - robot_platform/runtime/tests/host/test_safety_sensor_faults.c
  - robot_platform/runtime/tests/host/test_safety_arming.c
  - robot_platform/runtime/tests/host/test_safety_saturation.c
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03
  - SAFE-04
must_haves:
  truths:
    - Invalid command mapping or direction for the active profile fails Phase 2 explicitly instead of silently dispatching.
    - Stale, invalid, or unavailable sensor state cannot leave actuator output enabled on the current path.
    - Invalid start/arming transitions cannot escalate into `control_enable` and `actuator_enable` on the current path.
    - Saturation is judged as an explicit oracle with requirement-specific failure evidence, not a silent clamp.
  artifacts:
    - path: robot_platform/runtime/tests/host/test_safety_mapping.c
      provides: SAFE-01 command-mapping and direction regression coverage.
    - path: robot_platform/runtime/tests/host/test_safety_sensor_faults.c
      provides: SAFE-02 stale, invalid, and unavailable sensor regression coverage.
    - path: robot_platform/runtime/tests/host/test_safety_arming.c
      provides: SAFE-03 invalid arming and transition regression coverage.
    - path: robot_platform/runtime/tests/host/test_safety_saturation.c
      provides: SAFE-04 saturation oracle coverage.
  key_links:
    - from: robot_platform/runtime/control/controllers/balance_controller.c
      to: robot_platform/runtime/tests/host/test_safety_sensor_faults.c
      via: `start` / `control_enable` / `actuator_enable` contract output
      pattern: actuator_enable
    - from: robot_platform/runtime/control/execution/actuator_gateway.c
      to: robot_platform/runtime/tests/host/test_safety_mapping.c
      via: mapped device-command assertions
      pattern: platform_map_contract_command
    - from: robot_platform/runtime/control/constraints/actuator_constraints.c
      to: robot_platform/runtime/tests/host/test_safety_saturation.c
      via: explicit clamp oracle assertions
      pattern: WHEEL_TORQUE_MAX
---

<objective>
Turn the current control path’s implicit safety behavior into explicit hard-oracle judgments for mapping, sensor validity, arming, and saturation.

Purpose: Close SAFE-01 through SAFE-04 by making the real runtime path fail decisively when it would otherwise arm or dispatch unsafe output.
Output: Requirement-specific host regressions and runtime logic updates for mapping, sensor health, arming transitions, and saturation.
</objective>

<execution_context>
@/home/xbd/.codex/get-shit-done/workflows/execute-plan.md
@/home/xbd/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-host-safety-control-verification/02-CONTEXT.md
@.planning/phases/02-host-safety-control-verification/02-RESEARCH.md
@.planning/phases/02-host-safety-control-verification/02-01-PLAN.md
@robot_platform/runtime/control/controllers/balance_controller.c
@robot_platform/runtime/control/state/ins_state_estimator.c
@robot_platform/runtime/control/execution/actuator_gateway.c
@robot_platform/runtime/control/constraints/actuator_constraints.c
@robot_platform/runtime/app/balance_chassis/app_intent/remote_intent.c

<interfaces>
Current command-enable surface that needs real safety gating:
```c
state->actuator_command.start = (state->chassis.mode.start_enabled != 0U);
state->actuator_command.control_enable = (state->chassis.mode.start_enabled != 0U);
state->actuator_command.actuator_enable = (state->chassis.mode.start_enabled != 0U);
```

Current minimal dispatch gate:
```c
static bool platform_command_dispatch_enabled(const platform_actuator_command_t *actuator_msg)
{
    return actuator_msg->control_enable && actuator_msg->actuator_enable;
}
```

Current warmup readiness seam:
```c
if (state->ins_time > PLATFORM_INS_READY_WARMUP_TICKS)
{
    state->ins.health.ready = 1U;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add hard mapping and sensor-validity oracles on the current path</name>
  <read_first>
    - robot_platform/runtime/control/controllers/balance_controller.c
    - robot_platform/runtime/control/state/ins_state_estimator.c
    - robot_platform/runtime/control/execution/actuator_gateway.c
    - robot_platform/runtime/tests/host/test_support/balance_safety_harness.c
    - .planning/phases/02-host-safety-control-verification/02-RESEARCH.md
  </read_first>
  <files>robot_platform/runtime/control/controllers/balance_controller.c, robot_platform/runtime/control/state/ins_state_estimator.c, robot_platform/runtime/control/execution/actuator_gateway.c, robot_platform/runtime/tests/host/test_safety_mapping.c, robot_platform/runtime/tests/host/test_safety_sensor_faults.c</files>
  <behavior>
    - Test 1: SAFE-01 fails when a profile-invalid mapping or direction mismatch would otherwise reach a valid actuator command or device command.
    - Test 2: SAFE-02 fails when stale, invalid, or unavailable sensor state still leaves `control_enable` or `actuator_enable` asserted.
    - Test 3: Sensor warmup/readiness behavior is deterministic in tests and distinguishes pre-ready blocking from post-ready degradation.
  </behavior>
  <action>Implement requirement-specific host regressions for command mapping/direction and sensor freshness/validity using the new current-path harness from 02-01. Update `balance_controller`, `ins_state_estimator`, and `actuator_gateway` only as needed to make these behaviors explicit and machine-judgeable per D-14 through D-16. Pair enable-bit assertions with payload assertions so SAFE-01 is not reduced to `valid` flags alone, and do not hide stale-sensor failures behind startup flakiness or silent dispatch suppression.</action>
  <verify>
    <automated>ctest --test-dir build/robot_platform_host_tests --output-on-failure -R "test_safety_mapping|test_safety_sensor_faults"</automated>
  </verify>
  <acceptance_criteria>
    - Invalid mapping or direction produces an explicit failure instead of a silent pass.
    - Sensor-invalid and sensor-stale cases are deterministic and clearly classified.
    - Observed actuator output and enable bits are both asserted in the tests.
  </acceptance_criteria>
  <done>SAFE-01 and SAFE-02 are enforced by explicit host-side oracles on the real current path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explicit arming-transition and saturation oracles instead of silent behavior</name>
  <read_first>
    - robot_platform/runtime/app/balance_chassis/app_intent/remote_intent.c
    - robot_platform/runtime/control/controllers/balance_controller.c
    - robot_platform/runtime/control/constraints/actuator_constraints.c
    - robot_platform/runtime/tests/host/test_support/balance_safety_harness.c
  </read_first>
  <files>robot_platform/runtime/app/balance_chassis/app_intent/remote_intent.c, robot_platform/runtime/control/controllers/balance_controller.c, robot_platform/runtime/control/constraints/actuator_constraints.c, robot_platform/runtime/tests/host/test_safety_arming.c, robot_platform/runtime/tests/host/test_safety_saturation.c</files>
  <behavior>
    - Test 1: SAFE-03 fails when invalid arming or state transitions still propagate enabled actuator output.
    - Test 2: SAFE-04 fails when commanded wheel or leg outputs exceed configured limits or when clamping occurs without explicit oracle evidence.
    - Test 3: Recover, start, and jump-related transitions stay blocked unless the current path satisfies the intended gating conditions.
  </behavior>
  <action>Use the authoritative host harness to codify invalid arming/state-machine cases and explicit saturation cases. Tighten `remote_intent` and `balance_controller` so `start`, `control_enable`, and `actuator_enable` reflect real safety gating per D-18 rather than a bare `start_flag`. Convert silent clamp behavior in `actuator_constraints` into explicit testable oracles per D-15, but keep the runtime changes minimal and focused on verdict clarity instead of broader architecture cleanup.</action>
  <verify>
    <automated>ctest --test-dir build/robot_platform_host_tests --output-on-failure -R "test_safety_arming|test_safety_saturation"</automated>
  </verify>
  <acceptance_criteria>
    - Invalid transitions cannot produce enabled output on the current path.
    - Saturation coverage proves both the configured limits and the requirement-specific verdict.
    - The implementation stays tied to current `balance_chassis` behavior rather than introducing speculative state-machine redesign.
  </acceptance_criteria>
  <done>SAFE-03 and SAFE-04 are enforced by explicit host-side oracles on the current path.</done>
</task>

</tasks>

<verification>
Run `ctest --test-dir build/robot_platform_host_tests --output-on-failure -R "test_safety_mapping|test_safety_sensor_faults|test_safety_arming|test_safety_saturation"`. A green result proves the Phase 2 hard safety gates are enforced explicitly rather than inferred.
</verification>

<success_criteria>
The live `balance_chassis` host path fails deterministically when mapping, sensor validity, arming, or saturation behavior violates SAFE-01 through SAFE-04.
</success_criteria>

<output>
After completion, create `.planning/phases/02-host-safety-control-verification/02-02-SUMMARY.md`
</output>
