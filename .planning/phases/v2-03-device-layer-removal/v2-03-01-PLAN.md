---
phase: v2-03-device-layer-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - robot_platform/runtime/bsp/device_types.h
  - robot_platform/runtime/bsp/ports.h
  - robot_platform/runtime/bsp/boards/stm32h7_ctrl_board/bsp_ports_hw.c
  - robot_platform/runtime/bsp/sitl/bsp_ports_sitl.c
  - robot_platform/runtime/bsp/boards/stm32h7_ctrl_board/bsp_uart.c
  - robot_platform/runtime/bsp/boards/stm32h7_ctrl_board/can_bsp.c
  - robot_platform/runtime/bsp/sitl/BMI088driver_sitl.c
  - robot_platform/runtime/bsp/sitl/remote_control_sitl.c
  - robot_platform/runtime/bsp/sitl/dm4310_drv_sitl.c
  - robot_platform/runtime/control/execution/actuator_gateway.c
  - robot_platform/runtime/app/balance_chassis/app_bringup/remote_task.h
autonomous: true

must_haves:
  truths:
    - "BSP port implementations call BSP drivers directly, not device_layer"
    - "No source file outside runtime/device/ includes device_layer.h"
    - "device_types.h is accessible from bsp/ without referencing device/"
    - "actuator_gateway no longer calls platform_device_init_default_profile"
  artifacts:
    - path: "robot_platform/runtime/bsp/device_types.h"
      provides: "platform_device_result_t and related enums relocated from device/"
      contains: "platform_device_result_t"
    - path: "robot_platform/runtime/bsp/boards/stm32h7_ctrl_board/bsp_ports_hw.c"
      provides: "HW port implementation calling BSP drivers directly"
      contains: "BMI088_Read|get_remote_control_point|mit_ctrl"
    - path: "robot_platform/runtime/bsp/sitl/bsp_ports_sitl.c"
      provides: "SITL port implementation calling BSP driver stubs directly"
      contains: "BMI088_Read|get_remote_control_point|mit_ctrl"
  key_links:
    - from: "robot_platform/runtime/bsp/ports.h"
      to: "robot_platform/runtime/bsp/device_types.h"
      via: "#include"
      pattern: '#include "device_types.h"'
    - from: "robot_platform/runtime/bsp/boards/stm32h7_ctrl_board/bsp_ports_hw.c"
      to: "BSP driver headers (BMI088driver.h, remote_control.h, dm4310_drv.h)"
      via: "direct function calls"
      pattern: "BMI088_Read|get_remote_control_point"
---

<objective>
Rewire BSP port implementations to call BSP drivers directly and relocate device_types.h, severing all external dependencies on device_layer.h.

Purpose: After this plan, no code outside `runtime/device/` references device_layer — making the device directory safe to delete.
Output: Self-contained BSP port implementations, relocated device_types.h, cleaned actuator_gateway.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@robot_platform/runtime/bsp/ports.h
@robot_platform/runtime/bsp/boards/stm32h7_ctrl_board/bsp_ports_hw.c
@robot_platform/runtime/bsp/sitl/bsp_ports_sitl.c
@robot_platform/runtime/device/device_types.h
@robot_platform/runtime/device/device_layer.h
@robot_platform/runtime/device/device_layer.c
@robot_platform/runtime/device/device_profile_hw.c
@robot_platform/runtime/device/device_profile_sitl.c
@robot_platform/runtime/control/execution/actuator_gateway.c
@robot_platform/runtime/app/balance_chassis/app_bringup/remote_task.h
@robot_platform/runtime/bsp/sitl/BMI088driver_sitl.c
@robot_platform/runtime/bsp/sitl/remote_control_sitl.c
@robot_platform/runtime/bsp/sitl/dm4310_drv_sitl.c
@robot_platform/runtime/bsp/boards/stm32h7_ctrl_board/bsp_uart.c
@robot_platform/runtime/bsp/boards/stm32h7_ctrl_board/can_bsp.c
@robot_platform/CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate device_types.h and rewire includes</name>
  <files>
    robot_platform/runtime/bsp/device_types.h
    robot_platform/runtime/bsp/ports.h
    robot_platform/runtime/app/balance_chassis/app_bringup/remote_task.h
  </files>
  <action>
    1. Copy `runtime/device/device_types.h` to `runtime/bsp/device_types.h`. The content is identical — 3 enums and 1 struct (`platform_device_result_t`, `platform_device_backend_profile_t`, `platform_device_stamp_t`).

    2. `ports.h` already includes `"device_types.h"` — since the new file is in the same directory (`bsp/`), the include resolves without changes. Verify the include path is correct.

    3. Update `remote_task.h` line 7: change `#include "../../../device/device_types.h"` to `#include "../../../bsp/device_types.h"` (same relative depth, different subdirectory — both are under `runtime/`).

    4. Check for any other files outside `runtime/device/` that include `device_types.h` via a `device/` path. The grep shows only `remote_task.h` and `ports.h` — ports.h is already fine.
  </action>
  <verify>
    grep -r "device/device_types" robot_platform/runtime/ --include="*.h" --include="*.c" | grep -v "^robot_platform/runtime/device/" should return zero matches (only device-internal files may still reference the old path).
  </verify>
  <done>
    `device_types.h` exists at `runtime/bsp/device_types.h`. No file outside `runtime/device/` includes `device/device_types.h`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewire BSP port implementations and clean actuator_gateway</name>
  <files>
    robot_platform/runtime/bsp/boards/stm32h7_ctrl_board/bsp_ports_hw.c
    robot_platform/runtime/bsp/sitl/bsp_ports_sitl.c
    robot_platform/runtime/control/execution/actuator_gateway.c
    robot_platform/runtime/bsp/sitl/BMI088driver_sitl.c
    robot_platform/runtime/bsp/sitl/remote_control_sitl.c
    robot_platform/runtime/bsp/sitl/dm4310_drv_sitl.c
    robot_platform/runtime/bsp/boards/stm32h7_ctrl_board/bsp_uart.c
    robot_platform/runtime/bsp/boards/stm32h7_ctrl_board/can_bsp.c
  </files>
  <action>
    **bsp_ports_hw.c** — Complete rewrite. Remove `#include "device_layer.h"`. Instead include the BSP driver headers directly:
    - `BMI088driver.h` (for `BMI088_Read`, `BMI088` global)
    - `remote_control.h` (for `get_remote_control_point`, `RC_data_is_error`)
    - `dm4310_drv.h` (for `mit_ctrl`, `CAN_cmd_chassis`, `get_joint_motor_state`, `get_chassis_motor_measure_point`)
    - `fdcan.h` (for `hfdcan1`, `hfdcan2`)

    Implement the 4 port functions by calling BSP drivers directly, matching the logic currently in `device_profile_hw.c` bind functions and the device semantic wrappers. Specifically:
    - `platform_imu_read`: call `BMI088_Read(&BMI088)`, copy gyro/accel/temperature into `sample`
    - `platform_remote_read`: call `get_remote_control_point()`, check `RC_data_is_error()`, copy RC channels/switches/mouse/keyboard into `input`
    - `platform_motor_write_command`: iterate joints calling `mit_ctrl(hfdcan1, ...)` and wheels calling `CAN_cmd_chassis(hfdcan2, ...)`
    - `platform_motor_read_feedback`: call `get_joint_motor_state()` and `get_chassis_motor_measure_point()`, populate feedback struct

    Study the existing semantic wrapper implementations (`bmi088_device_hw.c`, `dbus_remote_device_hw.c`, `motor_actuator_device_hw.c`) to get the exact field mappings and driver call signatures. The port functions must produce identical data shapes.

    **bsp_ports_sitl.c** — Same approach. Remove `#include "device_layer.h"`. Include the SITL stub headers directly (`BMI088driver.h`, `remote_control.h`, `dm4310_drv.h`). Implement the same 4 functions calling the SITL stubs. The SITL stubs are simpler — they use UDP sockets internally but expose the same driver API.

    **BSP SITL stubs** — Update include paths in `BMI088driver_sitl.c`, `remote_control_sitl.c`, `dm4310_drv_sitl.c`: change `#include "../../device/..."` to include the driver headers from their current location. Since these driver headers (`BMI088driver.h`, `remote_control.h`, `dm4310_drv.h`) live in `device/` and will be deleted in Plan 02, the SITL stubs should include them via the CMake include path (which already adds the device subdirectories). Change to bare includes: `#include "BMI088driver.h"`, `#include "remote_control.h"`, `#include "dm4310_drv.h"`.

    **HW BSP files** — Update `bsp_uart.c` line 5: change `#include "../../../device/remote/dbus/remote_control.h"` to `#include "remote_control.h"` (already on include path). Update `can_bsp.c` line 5: change `#include "../../../device/actuator/motor/dm4310/dm4310_drv.h"` to `#include "dm4310_drv.h"` (already on include path).

    **actuator_gateway.c** — Remove `#include "../../device/device_layer.h"`. Remove the `platform_device_init_default_profile()` call from `platform_actuator_gateway_init()`. The function body becomes empty (or just a comment). Port implementations are now self-contained — no device_layer init needed.

    After all changes, verify no file outside `runtime/device/` includes `device_layer.h`:
    `grep -r "device_layer" robot_platform/runtime/ --include="*.c" --include="*.h" | grep -v "^robot_platform/runtime/device/"`
  </action>
  <verify>
    1. `grep -r "device_layer" robot_platform/runtime/ --include="*.c" --include="*.h" | grep -v "^robot_platform/runtime/device/"` returns zero matches
    2. `grep -r '#include.*device/' robot_platform/runtime/ --include="*.c" --include="*.h" | grep -v "^robot_platform/runtime/device/"` returns zero matches
    3. Build host tests: `cd robot_platform && cmake -B build_host_tests -DPLATFORM_HOST_TESTS=ON -DPLATFORM_TARGET_HW=OFF -DPLATFORM_TARGET_SIM=OFF && cmake --build build_host_tests 2>&1 | tail -20` — compiles without errors
    4. Run CTest: `cd robot_platform/build_host_tests && ctest --output-on-failure` — all tests pass
  </verify>
  <done>
    Both BSP port implementations call BSP drivers directly. No file outside `runtime/device/` references device_layer.h or any device/ path. actuator_gateway no longer calls device_layer init. All host tests pass.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "device_layer\|device_profile\|device_types" robot_platform/runtime/ --include="*.c" --include="*.h" | grep -v "^robot_platform/runtime/device/"` — only `bsp/device_types.h` should appear (the relocated copy)
2. Host test build and CTest pass with zero failures
3. `bsp_ports_hw.c` and `bsp_ports_sitl.c` contain no reference to `device_layer`
</verification>

<success_criteria>
- Zero external references to device_layer.h outside runtime/device/
- Zero external includes of any device/ path outside runtime/device/
- device_types.h relocated to bsp/ and all external consumers updated
- actuator_gateway.c has no device_layer dependency
- All CTest targets pass
</success_criteria>

<output>
After completion, create `.planning/phases/v2-03-device-layer-removal/v2-03-01-SUMMARY.md`
</output>
