---
phase: v2-03-device-layer-removal
plan: 02
type: execute
wave: 2
depends_on: ["v2-03-01"]
files_modified:
  - robot_platform/CMakeLists.txt
  - robot_platform/runtime/tests/host/test_actuator_gateway.c
  - robot_platform/runtime/tests/host/test_device_profile_safety_seams.c
  - robot_platform/runtime/tests/host/test_device_profile_sitl_runtime_bindings.c
  - robot_platform/runtime/device/
autonomous: true

must_haves:
  truths:
    - "All 34 files in runtime/device/ are deleted from the repository"
    - "No source file imports or includes anything from runtime/device/"
    - "CMakeLists.txt has no reference to PLATFORM_DEVICE_DIR or balance_chassis_device"
    - "test_device_profile_safety_seams and test_device_profile_sitl_runtime_bindings are removed"
    - "test_actuator_gateway no longer stubs platform_device_init_default_profile"
    - "Both HW ELF and SITL binary compile and link"
    - "All remaining CTest targets pass"
  artifacts:
    - path: "robot_platform/CMakeLists.txt"
      provides: "Clean CMake with no device_layer references"
    - path: "robot_platform/runtime/tests/host/test_actuator_gateway.c"
      provides: "Test without device_layer stub"
  key_links:
    - from: "robot_platform/CMakeLists.txt"
      to: "balance_chassis_hw_seed.elf"
      via: "target_link_libraries without balance_chassis_device"
      pattern: "balance_chassis_hw_seed\\.elf"
    - from: "robot_platform/CMakeLists.txt"
      to: "balance_chassis_sitl"
      via: "target_link_libraries without balance_chassis_device"
      pattern: "balance_chassis_sitl"
---

<objective>
Delete the entire device layer directory, remove the balance_chassis_device CMake library, clean up tests, and verify full build integrity.

Purpose: Complete the device_layer removal — the runtime/device/ directory ceases to exist, achieving the 2-hop indirection target (control -> BSP port -> BSP implementation).
Output: Clean repository with no device_layer artifacts, all builds and tests passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v2-03-device-layer-removal/v2-03-01-SUMMARY.md
@robot_platform/CMakeLists.txt
@robot_platform/runtime/tests/host/test_actuator_gateway.c
@robot_platform/runtime/tests/host/test_device_profile_safety_seams.c
@robot_platform/runtime/tests/host/test_device_profile_sitl_runtime_bindings.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean CMake and delete device_layer tests</name>
  <files>
    robot_platform/CMakeLists.txt
    robot_platform/runtime/tests/host/test_actuator_gateway.c
    robot_platform/runtime/tests/host/test_device_profile_safety_seams.c
    robot_platform/runtime/tests/host/test_device_profile_sitl_runtime_bindings.c
  </files>
  <action>
    **CMakeLists.txt changes:**

    1. Remove the `PLATFORM_DEVICE_DIR` variable and all derived variables (`PLATFORM_DEVICE_REMOTE_DIR`, `PLATFORM_DEVICE_MOTOR_DIR`, `PLATFORM_DEVICE_IMU_DIR`) — lines 46-49.

    2. Remove the entire `balance_chassis_device` library block (lines 946-1019): the `if((PLATFORM_TARGET_HW OR PLATFORM_TARGET_SIM) AND NOT TARGET balance_chassis_device)` block and everything inside it.

    3. Remove `BALANCE_CHASSIS_DEVICE_SOURCES` variable (line 57) and the two `list(APPEND BALANCE_CHASSIS_DEVICE_SOURCES ...)` blocks for HW (lines 541-548) and SIM (lines 550-558).

    4. Remove `balance_chassis_device` from `target_link_libraries` of `balance_chassis_hw_seed.elf` (line 747) and `balance_chassis_sitl` (line 940).

    5. Remove all `${PLATFORM_DEVICE_DIR}` include directories from every target:
       - `balance_chassis_bsp_seed` target_include_directories: remove lines referencing `${PLATFORM_DEVICE_DIR}`, `${PLATFORM_DEVICE_DIR}/imu`, `${PLATFORM_DEVICE_DIR}/remote`, `${PLATFORM_DEVICE_DIR}/actuator`, `${PLATFORM_DEVICE_DIR}/actuator/motor`, `${PLATFORM_DEVICE_IMU_DIR}`, `${PLATFORM_DEVICE_REMOTE_DIR}`, `${PLATFORM_DEVICE_MOTOR_DIR}`
       - `balance_chassis_generated_raw` target_include_directories: remove `${PLATFORM_DEVICE_REMOTE_DIR}`, `${PLATFORM_DEVICE_MOTOR_DIR}`
       - `balance_chassis_hw_seed.elf` target_include_directories: remove all `${PLATFORM_DEVICE_DIR}` lines and `${PLATFORM_DEVICE_REMOTE_DIR}`, `${PLATFORM_DEVICE_MOTOR_DIR}`, `${PLATFORM_DEVICE_IMU_DIR}`
       - `balance_chassis_sitl` target_include_directories: remove `${PLATFORM_DEVICE_MOTOR_DIR}`, `${PLATFORM_DEVICE_IMU_DIR}`, `${PLATFORM_DEVICE_REMOTE_DIR}`
       - `balance_safety_host_runtime` target_include_directories: remove all `${PLATFORM_DEVICE_DIR}` lines
       - `test_actuator_gateway` target_include_directories: remove `${PLATFORM_RUNTIME_DIR}/device`

    6. Remove the 5 device source files from `BALANCE_SAFETY_HOST_RUNTIME_SOURCES` (lines 237-241):
       - `${PLATFORM_RUNTIME_DIR}/device/device_profile_sitl.c`
       - `${PLATFORM_RUNTIME_DIR}/device/device_layer.c`
       - `${PLATFORM_RUNTIME_DIR}/device/imu/bmi088_device_sitl.c`
       - `${PLATFORM_RUNTIME_DIR}/device/remote/dbus_remote_device_sitl.c`
       - `${PLATFORM_RUNTIME_DIR}/device/actuator/motor/motor_actuator_device_sitl.c`

    7. Remove the driver source files from `BALANCE_CHASSIS_BSP_SEED_SOURCES` that reference device paths (lines 621-622):
       - `${PLATFORM_DEVICE_REMOTE_DIR}/remote_control.c`
       - `${PLATFORM_DEVICE_MOTOR_DIR}/dm4310_drv.c`
       These driver files need to be added back using BSP-relative paths. The actual `.c` files still live in `device/` and will be deleted — but they are low-level driver code that BSP needs. Before deleting device/, copy these 3 driver source files to BSP:
       - `device/remote/dbus/remote_control.c` -> `bsp/boards/stm32h7_ctrl_board/remote_control.c` (already has the `.h` on include path)
       - `device/actuator/motor/dm4310/dm4310_drv.c` -> `bsp/boards/stm32h7_ctrl_board/dm4310_drv.c`
       - `device/imu/bmi088/BMI088driver.c` -> `bsp/boards/stm32h7_ctrl_board/BMI088driver.c`
       - `device/imu/bmi088/BMI088Middleware.c` -> `bsp/boards/stm32h7_ctrl_board/BMI088Middleware.c`
       Also copy the corresponding headers:
       - `device/remote/dbus/remote_control.h` -> `bsp/boards/stm32h7_ctrl_board/remote_control.h`
       - `device/remote/dbus/struct_typedef.h` -> `bsp/boards/stm32h7_ctrl_board/struct_typedef.h`
       - `device/actuator/motor/dm4310/dm4310_drv.h` -> `bsp/boards/stm32h7_ctrl_board/dm4310_drv.h`
       - `device/imu/bmi088/BMI088driver.h` -> `bsp/boards/stm32h7_ctrl_board/BMI088driver.h`
       - `device/imu/bmi088/BMI088Middleware.h` -> `bsp/boards/stm32h7_ctrl_board/BMI088Middleware.h`
       - `device/imu/bmi088/BMI088reg.h` -> `bsp/boards/stm32h7_ctrl_board/BMI088reg.h`
       Update `BALANCE_CHASSIS_BSP_SEED_SOURCES` to reference the new locations.
       Update `balance_chassis_generated_raw` include directories to use `${PLATFORM_BOARD_BSP_DIR}` instead of `${PLATFORM_DEVICE_REMOTE_DIR}` and `${PLATFORM_DEVICE_MOTOR_DIR}` (it already includes `${PLATFORM_BOARD_BSP_DIR}`).

    8. Delete the `test_device_profile_safety_seams` executable and test blocks entirely — this test exercises device_layer internals (vtable binding, hook API) that no longer exist.

    9. Delete the `test_device_profile_sitl_runtime_bindings` executable and test blocks entirely — this test verifies device_layer_init_profile and layer struct fields that no longer exist.

    **test_actuator_gateway.c changes:**

    Remove the `platform_device_init_default_profile` stub function (lines 9-15) and the `g_init_call_count` variable (line 9). Remove `test_init_calls_default_profile_once` test function and its call from `main()`. The actuator_gateway_init function is now a no-op, so there's nothing to test about init.

    **Delete test files:**
    - `rm robot_platform/runtime/tests/host/test_device_profile_safety_seams.c`
    - `rm robot_platform/runtime/tests/host/test_device_profile_sitl_runtime_bindings.c`
  </action>
  <verify>
    1. `grep -rn "PLATFORM_DEVICE_DIR\|balance_chassis_device\|device_layer\|device_profile_safety\|device_profile_sitl_runtime" robot_platform/CMakeLists.txt` returns zero matches
    2. `grep -rn "platform_device_init_default_profile" robot_platform/runtime/tests/host/test_actuator_gateway.c` returns zero matches
    3. `ls robot_platform/runtime/tests/host/test_device_profile_safety_seams.c 2>&1` returns "No such file"
    4. `ls robot_platform/runtime/tests/host/test_device_profile_sitl_runtime_bindings.c 2>&1` returns "No such file"
  </verify>
  <done>
    CMakeLists.txt has no device_layer references. Driver files relocated to BSP. Device-layer-specific tests deleted. test_actuator_gateway cleaned of device_layer stub.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete runtime/device/ and verify full build</name>
  <files>
    robot_platform/runtime/device/
  </files>
  <action>
    1. Delete the entire `runtime/device/` directory: `rm -rf robot_platform/runtime/device/`

    2. Verify no dangling references: `grep -rn "runtime/device\|device_layer\|device_profile\b" robot_platform/ --include="*.c" --include="*.h" --include="*.cmake" --include="CMakeLists.txt" | grep -v "build_"` should return zero matches (excluding build artifacts).

    3. Build host tests:
       ```
       cd robot_platform
       rm -rf build_host_tests
       cmake -B build_host_tests -DPLATFORM_HOST_TESTS=ON -DPLATFORM_TARGET_HW=OFF -DPLATFORM_TARGET_SIM=OFF
       cmake --build build_host_tests
       ```

    4. Run all CTest targets:
       ```
       cd robot_platform/build_host_tests
       ctest --output-on-failure
       ```
       Expected: All remaining tests pass (test_device_profile_safety_seams and test_device_profile_sitl_runtime_bindings no longer exist, so test count drops by 2).

    5. Build SITL binary:
       ```
       cd robot_platform
       rm -rf build_sitl
       cmake -B build_sitl -DPLATFORM_TARGET_HW=OFF -DPLATFORM_TARGET_SIM=ON -DPLATFORM_HOST_TESTS=OFF
       cmake --build build_sitl
       ```
       Expected: `balance_chassis_sitl` links successfully.

    6. Build HW ELF (if cross-compiler available):
       ```
       cd robot_platform
       rm -rf build_hw
       cmake -B build_hw -DPLATFORM_TARGET_HW=ON -DPLATFORM_TARGET_SIM=OFF -DPLATFORM_HOST_TESTS=OFF -DCMAKE_TOOLCHAIN_FILE=cmake/toolchains/arm-none-eabi-gcc.cmake
       cmake --build build_hw
       ```
       Expected: `balance_chassis_hw_seed.elf` links successfully. If cross-compiler is not available, note this as a known gap and verify SITL + host tests are sufficient.

    7. Perform call-chain audit: verify 2-hop indirection by tracing one path:
       - `ins_task.c` calls `platform_imu_read()` (hop 1: control -> BSP port)
       - `bsp_ports_hw.c::platform_imu_read()` calls `BMI088_Read()` (hop 2: BSP port -> BSP driver)
       No intermediate device_layer hop exists.
  </action>
  <verify>
    1. `ls robot_platform/runtime/device/ 2>&1` returns "No such file or directory"
    2. `grep -rn "device_layer\|device_profile\|runtime/device" robot_platform/ --include="*.c" --include="*.h" --include="CMakeLists.txt" | grep -v build_ | wc -l` returns 0
    3. Host tests build and all CTest targets pass
    4. SITL binary builds and links
    5. HW ELF builds and links (or documented as environment-dependent)
  </verify>
  <done>
    runtime/device/ directory is fully deleted. No dangling references anywhere. HW ELF and SITL binary compile and link. All CTest targets pass. Control-to-hardware indirection is exactly 2 hops.
  </done>
</task>

</tasks>

<verification>
1. `find robot_platform/runtime/device -type f 2>&1` returns error (directory gone)
2. `grep -rn "device_layer\|PLATFORM_DEVICE_DIR\|balance_chassis_device" robot_platform/ --include="*.c" --include="*.h" --include="CMakeLists.txt" | grep -v build_` returns zero matches
3. Host test build + CTest: all pass
4. SITL build: links successfully
5. HW ELF build: links successfully
6. Call-chain audit confirms 2-hop pattern for all 4 port functions
</verification>

<success_criteria>
- All 34 files in runtime/device/ deleted
- No source file imports or includes anything from runtime/device/
- CMakeLists.txt has no PLATFORM_DEVICE_DIR or balance_chassis_device references
- Driver source/header files relocated to bsp/boards/stm32h7_ctrl_board/
- Both HW ELF and SITL binary compile and link
- All remaining CTest targets pass (count reduced by 2 from deleted device-layer tests)
- Control code to hardware indirection is 2 hops
</success_criteria>

<output>
After completion, create `.planning/phases/v2-03-device-layer-removal/v2-03-02-SUMMARY.md`
</output>
