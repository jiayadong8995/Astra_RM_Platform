---
phase: 03-fake-link-runtime-proof
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - robot_platform/sim/backends/sitl_bridge.py
  - robot_platform/sim/core/runner.py
  - robot_platform/sim/projects/balance_chassis/profile.py
  - robot_platform/sim/projects/balance_chassis/validation.py
  - robot_platform/sim/tests/test_runner.py
  - robot_platform/tools/platform_cli/main.py
  - robot_platform/tools/platform_cli/tests/test_main.py
autonomous: true
requirements:
  - LINK-03
  - LINK-04
  - OBS-02
must_haves:
  truths:
    - "Verification artifacts classify failures as communication, control, or observation instead of collapsing them into a generic smoke failure."
    - "Contract drift in topic boundaries, protocol, or transport ports fails Phase 3 explicitly."
    - "Artifacts expose diagnostics for dropped packets, stale inputs, or missing runtime observations."
    - "Classification logic reinforces the thin application-layer proof model instead of expanding simulator fidelity scope."
  artifacts:
    - path: robot_platform/sim/core/runner.py
      provides: "Failure-layer classification and diagnostics summarization."
    - path: robot_platform/tools/platform_cli/main.py
      provides: "Phase 3 case matrix covering classification, contract drift, and diagnostics."
    - path: robot_platform/sim/tests/test_runner.py
      provides: "Regression coverage for contract mismatch and failure-layer logic."
  key_links:
    - from: robot_platform/sim/core/runner.py
      to: robot_platform/tools/platform_cli/main.py
      via: "smoke_result consumed by verify phase3"
      pattern: "failure_layer|failure_reason|communication_diagnostics"
    - from: robot_platform/sim/backends/sitl_bridge.py
      to: robot_platform/sim/core/runner.py
      via: "bridge counters and adapter binding events"
      pattern: "stats|adapter_binding"
---

<objective>
Finish the Phase 3 proof by classifying failures cleanly, failing explicit contract drift, and surfacing diagnostics that separate communication faults from control faults.

Purpose: Satisfy D-01, D-02, D-03, D-05, D-09, D-10, D-11, D-13, and D-15 by making Phase 3 artifacts localize failures by layer while preserving machine-readable evidence and avoiding simulator-fidelity drift.
Output: Finalized runner classification logic, explicit contract-drift failures, richer diagnostics, and completed `verify phase3` case coverage.
</objective>

<execution_context>
@/home/xbd/.codex/get-shit-done/workflows/execute-plan.md
@/home/xbd/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fake-link-runtime-proof/03-CONTEXT.md
@.planning/phases/03-fake-link-runtime-proof/03-RESEARCH.md
@.planning/phases/03-fake-link-runtime-proof/03-VALIDATION.md
@robot_platform/sim/core/runner.py
@robot_platform/sim/projects/balance_chassis/profile.py
@robot_platform/sim/projects/balance_chassis/validation.py
@robot_platform/sim/backends/sitl_bridge.py
@robot_platform/tools/platform_cli/main.py
@robot_platform/runtime/control/state/observe_task.c
@robot_platform/runtime/app/balance_chassis/app_intent/remote_intent.c

<interfaces>
From `robot_platform/sim/core/runner.py`:
```python
def _summarize_runtime_boundary(summary: dict[str, object]) -> None: ...
def _build_smoke_result(summary: dict[str, object]) -> None: ...
```

From `robot_platform/sim/projects/balance_chassis/validation.py`:
```python
def build_validation_status(summary: dict[str, object],
                            profile: SimProjectProfile) -> list[ValidationStatus]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression coverage for failure-layer classification and contract drift</name>
  <files>
robot_platform/sim/tests/test_runner.py
robot_platform/tools/platform_cli/tests/test_main.py
  </files>
  <read_first>
robot_platform/sim/tests/test_runner.py
robot_platform/tools/platform_cli/tests/test_main.py
robot_platform/sim/core/runner.py
robot_platform/tools/platform_cli/main.py
robot_platform/sim/projects/balance_chassis/profile.py
robot_platform/sim/projects/balance_chassis/validation.py
  </read_first>
  <behavior>
    - Test 1: mismatched protocol, runtime boundary, or transport ports produce a `failure_layer` of `communication` and explicit drift reasons.
    - Test 2: missing runtime output observations with healthy transport produce `failure_layer` `observation`, while unsafe actuator verdicts produce `failure_layer` `control`.
    - Test 3: diagnostics payloads preserve counters and freshness/missing-observation reasons for OBS-02.
  </behavior>
  <action>Create failing regressions that pin the final Phase 3 schema. Per D-01 through D-03, the failure layers should describe proof across the thin application/runtime responsibility chain rather than imply new architecture ownership. In `robot_platform/sim/tests/test_runner.py`, add summary tests for exact strings `failure_layer`, `transport_ports_mismatch`, `runtime_boundary_mismatch`, `protocol_mismatch`, `communication_diagnostics`, `observation_diagnostics`, and `control_diagnostics`. In `robot_platform/tools/platform_cli/tests/test_main.py`, add `VerifyPhase3Tests` cases for `classification`, `contract_drift`, and `diagnostics`, and assert the Phase 3 JSON artifact returns these cases in the final `cases` list.</action>
  <acceptance_criteria>
    - `robot_platform/sim/tests/test_runner.py` contains `failure_layer`, `communication_diagnostics`, `observation_diagnostics`, and `control_diagnostics`.
    - `robot_platform/tools/platform_cli/tests/test_main.py` contains `classification`, `contract_drift`, and `diagnostics`.
    - `rg -n "transport_ports_mismatch|runtime_boundary_mismatch|protocol_mismatch" robot_platform/sim/tests/test_runner.py` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest robot_platform.sim.tests.test_runner robot_platform.tools.platform_cli.tests.test_main -v</automated>
  </verify>
  <done>There are automated expectations for the exact Phase 3 failure-layer and contract-drift schema before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement failure-layer classification, contract-drift refusal, and diagnostics summaries</name>
  <files>
robot_platform/sim/backends/sitl_bridge.py
robot_platform/sim/core/runner.py
robot_platform/sim/projects/balance_chassis/profile.py
robot_platform/sim/projects/balance_chassis/validation.py
robot_platform/tools/platform_cli/main.py
  </files>
  <read_first>
robot_platform/sim/backends/sitl_bridge.py
robot_platform/sim/core/runner.py
robot_platform/sim/projects/balance_chassis/profile.py
robot_platform/sim/projects/balance_chassis/validation.py
robot_platform/tools/platform_cli/main.py
.planning/phases/03-fake-link-runtime-proof/03-CONTEXT.md
robot_platform/runtime/control/state/observe_task.c
robot_platform/runtime/app/balance_chassis/app_intent/remote_intent.c
  </read_first>
  <behavior>
    - Test 1: bridge protocol/runtime-boundary/port drift fails verification explicitly even if the SITL session stayed alive.
    - Test 2: diagnostics preserve bridge counters, stale-input indications, and missing-runtime-observation counts.
    - Test 3: final verification artifacts classify failures into `communication`, `control`, or `observation`.
  </behavior>
  <action>Complete the Phase 3 artifact logic. In `runner.py`, derive `failure_layer` using explicit precedence across the thin application/runtime proof frame from D-01 through D-03: contract drift or adapter binding failure => `communication`; required runtime output missing while transport is healthy => `observation`; validation target or control verdict failure with observed runtime data => `control`. Read `observe_task.c` and `remote_intent.c` first so observation-related failures continue to reflect D-05, where state observation is a peer input to intent/mode decisions instead of a downstream afterthought. Persist exact nested objects `communication_diagnostics`, `observation_diagnostics`, and `control_diagnostics`, each with stable keys and machine-readable reasons. Treat declared-vs-observed protocol, boundary, and transport-port mismatches as hard failures per D-15, not warnings. Promote bridge counters, stale remote freshness state, and missing runtime-output counts into `smoke_result`. In `main.py`, extend `PHASE3_CASES` and `_run_verify_phase3` so the case matrix includes `classification`, `contract_drift`, and `diagnostics`, and fail the command when any required Phase 3 case or contract check fails. Do not broaden this work into higher-fidelity simulator behavior, because D-09 keeps proof and observability ahead of realism.</action>
  <acceptance_criteria>
    - `robot_platform/sim/core/runner.py` contains `failure_layer =` and the exact strings `communication`, `control`, and `observation`.
    - `robot_platform/sim/core/runner.py` contains `communication_diagnostics`, `observation_diagnostics`, and `control_diagnostics`.
    - `robot_platform/tools/platform_cli/main.py` contains `classification`, `contract_drift`, and `diagnostics` in `PHASE3_CASES`.
    - `robot_platform/sim/core/runner.py` contains at least one explicit `mismatch` reason string for protocol, runtime boundary, and transport ports.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m robot_platform.tools.platform_cli.main verify phase3</automated>
  </verify>
  <done>`LINK-03`, `LINK-04`, and `OBS-02` are satisfied with explicit contract-drift failures, failure-layer classification, and machine-readable diagnostics in the authoritative Phase 3 artifact.</done>
</task>

</tasks>

<verification>
Use the quick Python regression command after each task, then close the plan with the full Phase 3 verification command from `03-VALIDATION.md`.
</verification>

<success_criteria>
Running `python3 -m robot_platform.tools.platform_cli.main verify phase3` produces a JSON artifact that distinguishes communication/control/observation failures, fails explicit declaration drift, and exposes counters or freshness diagnostics for packet loss and missing observations.
</success_criteria>

<output>
After completion, create `.planning/phases/03-fake-link-runtime-proof/03-03-SUMMARY.md`
</output>
