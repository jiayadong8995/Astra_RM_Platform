---
phase: 03-fake-link-runtime-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - robot_platform/CMakeLists.txt
  - robot_platform/runtime/bsp/sitl/BMI088driver_sitl.c
  - robot_platform/runtime/bsp/sitl/remote_control_sitl.c
  - robot_platform/runtime/device/device_profile_sitl.c
  - robot_platform/runtime/device/imu/bmi088_device_sitl.c
  - robot_platform/runtime/device/remote/dbus_remote_device_sitl.c
  - robot_platform/runtime/tests/host/test_device_profile_sitl_runtime_bindings.c
autonomous: true
requirements:
  - LINK-01
must_haves:
  truths:
    - "Fake-link ingress for SITL IMU and remote data reaches the existing runtime device path instead of stub-only placeholders."
    - "The authoritative runtime chain still starts from the current remote/state inputs and not from a parallel sim-only controller."
  artifacts:
    - path: robot_platform/runtime/device/device_profile_sitl.c
      provides: "SITL profile binds runtime-backed IMU and remote adapters."
    - path: robot_platform/runtime/bsp/sitl/remote_control_sitl.c
      provides: "UDP-backed SITL remote ingress feeding the existing remote-control state surface."
    - path: robot_platform/runtime/tests/host/test_device_profile_sitl_runtime_bindings.c
      provides: "Regression proving the SITL profile no longer uses stub-only IMU/remote bindings."
  key_links:
    - from: robot_platform/runtime/device/device_profile_sitl.c
      to: robot_platform/runtime/bsp/sitl/BMI088driver_sitl.c
      via: "config-backed IMU bind"
      pattern: "BMI088_Init|BMI088_Read"
    - from: robot_platform/runtime/device/device_profile_sitl.c
      to: robot_platform/runtime/bsp/sitl/remote_control_sitl.c
      via: "config-backed remote bind"
      pattern: "get_remote_control_point|RC_data_is_error"
---

<objective>
Make the SITL fake-link ingress honest by binding IMU and remote input through runtime-backed adapters instead of ready-by-default stub devices.

Purpose: Satisfy D-06, D-07, D-08, and D-14 by proving that Phase 3 still drives the real `remote_task -> Observe_task -> Chassis_task -> motor_control_task` path.
Output: Runtime-backed SITL device bindings plus a host regression that fails if the profile falls back to stub-only IMU/remote behavior.
</objective>

<execution_context>
@/home/xbd/.codex/get-shit-done/workflows/execute-plan.md
@/home/xbd/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fake-link-runtime-proof/03-CONTEXT.md
@.planning/phases/03-fake-link-runtime-proof/03-RESEARCH.md
@.planning/phases/02-host-safety-control-verification/02-01-SUMMARY.md
@robot_platform/runtime/device/device_profile_sitl.c
@robot_platform/runtime/device/device_profile_hw.c
@robot_platform/runtime/bsp/sitl/BMI088driver_sitl.c
@robot_platform/runtime/device/imu/bmi088_device_hw.c
@robot_platform/runtime/device/remote/dbus_remote_device_hw.c
@robot_platform/runtime/device/remote/dbus/remote_control.h
@robot_platform/runtime/device/device_layer.c

<interfaces>
From `robot_platform/runtime/device/device_profile.h`:
```c
typedef struct platform_device_profile {
  const char *name;
  void (*bind_imu)(platform_device_layer_t *layer);
  void (*bind_remote)(platform_device_layer_t *layer);
  void (*bind_motor)(platform_device_layer_t *layer);
} platform_device_profile_t;
```

From `robot_platform/runtime/device/device_layer.h`:
```c
platform_device_result_t platform_device_layer_init_profile(platform_device_layer_t *layer,
                                                            platform_device_backend_profile_t profile);
platform_device_result_t platform_device_layer_read_remote(platform_device_layer_t *layer,
                                                           platform_rc_input_t *input);
platform_device_result_t platform_device_layer_read_imu(platform_device_layer_t *layer,
                                                        platform_imu_sample_t *sample);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression coverage for runtime-backed SITL profile binding</name>
  <files>
robot_platform/CMakeLists.txt
robot_platform/runtime/tests/host/test_device_profile_sitl_runtime_bindings.c
  </files>
  <read_first>
robot_platform/CMakeLists.txt
robot_platform/runtime/tests/host/test_device_profile_safety_seams.c
robot_platform/runtime/device/device_profile_sitl.c
robot_platform/runtime/device/device_profile_hw.c
robot_platform/runtime/device/device_layer.c
  </read_first>
  <behavior>
    - Test 1: `platform_device_layer_init_profile(..., PLATFORM_DEVICE_BACKEND_PROFILE_SITL)` exposes IMU and remote device names that are not the legacy `*_sitl_stub` identifiers.
    - Test 2: SITL profile initialization succeeds while using the runtime-backed IMU and remote acquire functions expected by D-06 and D-08.
  </behavior>
  <action>Per D-06 and D-08, add a dedicated host regression target that initializes a local SITL `platform_device_layer_t`, asserts the bound IMU name is exactly `bmi088_sitl_udp`, asserts the bound remote name is exactly `dbus_remote_sitl_udp`, and proves both ops tables expose non-null init/read callbacks. Wire the new test target into the existing host-test build in `robot_platform/CMakeLists.txt` beside the other Phase 2 host regressions; do not reuse `test_device_profile_safety_seams.c`, because this phase needs a separate guard that fails specifically on Phase 3 binding drift.</action>
  <acceptance_criteria>
    - `robot_platform/runtime/tests/host/test_device_profile_sitl_runtime_bindings.c` contains the exact strings `bmi088_sitl_udp` and `dbus_remote_sitl_udp`.
    - `robot_platform/CMakeLists.txt` contains `test_device_profile_sitl_runtime_bindings`.
    - `rg -n "sitl_stub" robot_platform/runtime/tests/host/test_device_profile_sitl_runtime_bindings.c` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>ctest --test-dir build/robot_platform_host_tests --output-on-failure -R test_device_profile_sitl_runtime_bindings</automated>
  </verify>
  <done>The repository has an automated guard that fails if the SITL device profile reverts to stub-only IMU or remote bindings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebind SITL IMU and remote ingress to runtime-backed adapters</name>
  <files>
robot_platform/CMakeLists.txt
robot_platform/runtime/bsp/sitl/BMI088driver_sitl.c
robot_platform/runtime/bsp/sitl/remote_control_sitl.c
robot_platform/runtime/device/device_profile_sitl.c
robot_platform/runtime/device/imu/bmi088_device_sitl.c
robot_platform/runtime/device/remote/dbus_remote_device_sitl.c
  </files>
  <read_first>
robot_platform/runtime/bsp/sitl/BMI088driver_sitl.c
robot_platform/runtime/device/device_profile_sitl.c
robot_platform/runtime/device/device_profile_hw.c
robot_platform/runtime/device/imu/bmi088_device_hw.c
robot_platform/runtime/device/remote/dbus_remote_device_hw.c
robot_platform/runtime/device/remote/dbus/remote_control.h
robot_platform/runtime/app/balance_chassis/app_bringup/remote_task.c
robot_platform/runtime/control/state/ins_task.c
  </read_first>
  <behavior>
    - Test 1: SITL IMU binding uses the existing UDP-fed `BMI088driver_sitl.c` path rather than the hardcoded gravity-only sample generator.
    - Test 2: SITL remote binding reads from a runtime-backed `RC_ctrl_t` source rather than the hardcoded switch/channel stub returned by the current SITL device wrapper.
  </behavior>
  <action>Replace the current `*_sitl_stub` wrappers with config-backed bindings modeled on `device_profile_hw.c`: keep the motor path unchanged, but define static SITL config objects in `device_profile_sitl.c` so IMU uses `BMI088_Init`/`BMI088_Read` from `robot_platform/runtime/bsp/sitl/BMI088driver_sitl.c`, and remote uses a new `robot_platform/runtime/bsp/sitl/remote_control_sitl.c` that exports `get_remote_control_point()` and `RC_data_is_error()` for UDP-fed fake-link input. Rename the SITL device names to `bmi088_sitl_udp` and `dbus_remote_sitl_udp`, keep the runtime chain authoritative per D-06, and do not introduce any alternate controller logic or Python-owned control path.</action>
  <acceptance_criteria>
    - `robot_platform/runtime/device/device_profile_sitl.c` contains `BMI088_Init`, `BMI088_Read`, `get_remote_control_point`, and `RC_data_is_error`.
    - `robot_platform/runtime/device/imu/bmi088_device_sitl.c` contains the exact string `bmi088_sitl_udp`.
    - `robot_platform/runtime/device/remote/dbus_remote_device_sitl.c` contains the exact string `dbus_remote_sitl_udp`.
    - `robot_platform/runtime/bsp/sitl/remote_control_sitl.c` exists and contains both `get_remote_control_point` and `RC_data_is_error`.
    - `rg -n "dbus_remote_sitl_stub|bmi088_sitl_stub" robot_platform/runtime/device` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>ctest --test-dir build/robot_platform_host_tests --output-on-failure -R test_device_profile_sitl_runtime_bindings</automated>
  </verify>
  <done>SITL fake-link ingress is bound through runtime-backed IMU and remote adapters, and the binding proof is enforced by host automation.</done>
</task>

</tasks>

<verification>
Run the dedicated host binding regression after each task. The plan is complete only when the SITL profile exposes runtime-backed IMU and remote bindings with no remaining `*_sitl_stub` identifiers.
</verification>

<success_criteria>
`LINK-01` is satisfied once the SITL profile can no longer initialize with stub-only IMU/remote adapters and the host regression proves those bindings use the runtime-backed path.
</success_criteria>

<output>
After completion, create `.planning/phases/03-fake-link-runtime-proof/03-01-SUMMARY.md`
</output>
