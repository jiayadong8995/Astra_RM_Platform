---
phase: 03-fake-link-runtime-proof
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - robot_platform/runtime/app/balance_chassis/app_io/chassis_topics.c
  - robot_platform/sim/backends/sitl_bridge.py
  - robot_platform/sim/core/profile.py
  - robot_platform/sim/core/runner.py
  - robot_platform/sim/projects/balance_chassis/profile.py
  - robot_platform/sim/projects/balance_chassis/validation.py
  - robot_platform/sim/tests/test_runner.py
  - robot_platform/tools/platform_cli/main.py
  - robot_platform/tools/platform_cli/tests/test_main.py
autonomous: true
requirements:
  - LINK-01
  - LINK-02
  - OBS-01
must_haves:
  truths:
    - "Developer can run `verify phase3 --case runtime_binding` and get machine-readable proof that fake-link inputs drive the authoritative runtime chain."
    - "Developer can run `verify phase3 --case runtime_outputs` and see observed runtime outputs instead of declared-only expectations."
    - "Developer can inspect one authoritative JSON artifact that records adapter-binding status and verification outcomes."
  artifacts:
    - path: robot_platform/tools/platform_cli/main.py
      provides: "Dedicated `verify phase3` command and case matrix."
    - path: robot_platform/sim/core/runner.py
      provides: "Smoke summary fields for adapter binding and runtime-output observations."
    - path: robot_platform/sim/tests/test_runner.py
      provides: "Regression coverage for the Phase 3 artifact schema."
  key_links:
    - from: robot_platform/runtime/app/balance_chassis/app_io/chassis_topics.c
      to: robot_platform/sim/core/runner.py
      via: "runtime output observation lines"
      pattern: "RuntimeOutput|runtime_output_observations"
    - from: robot_platform/sim/backends/sitl_bridge.py
      to: robot_platform/tools/platform_cli/main.py
      via: "Phase 3 smoke artifact consumed by verify phase3"
      pattern: "adapter_binding|phase3"
---

<objective>
Create the Phase 3 machine-readable proof surface so runtime outputs and adapter-binding truth appear in authoritative artifacts, not just in raw smoke logs.

Purpose: Satisfy D-01, D-02, D-03, D-04, D-05, D-09, and D-12 through D-16 by making JSON artifacts the first proof surface for runtime-path execution and binding status without expanding simulator realism scope.
Output: `verify phase3`, extended smoke summary fields, and automated tests for runtime-output and artifact-schema cases.
</objective>

<execution_context>
@/home/xbd/.codex/get-shit-done/workflows/execute-plan.md
@/home/xbd/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fake-link-runtime-proof/03-CONTEXT.md
@.planning/phases/03-fake-link-runtime-proof/03-RESEARCH.md
@.planning/phases/01-contracts-and-verification-foundation/01-04-SUMMARY.md
@.planning/phases/02-host-safety-control-verification/02-03-SUMMARY.md
@robot_platform/tools/platform_cli/main.py
@robot_platform/sim/core/profile.py
@robot_platform/sim/core/runner.py
@robot_platform/sim/projects/balance_chassis/profile.py
@robot_platform/sim/projects/balance_chassis/validation.py
@robot_platform/sim/backends/sitl_bridge.py
@robot_platform/runtime/app/balance_chassis/app_io/chassis_topics.c
@robot_platform/runtime/control/state/observe_task.c
@robot_platform/runtime/app/balance_chassis/app_intent/remote_intent.c
@robot_platform/runtime/app/balance_chassis/app_bringup/remote_task.c
@robot_platform/runtime/app/balance_chassis/app_bringup/chassis_task.c
@robot_platform/runtime/control/execution/motor_control_task.c

<interfaces>
From `robot_platform/sim/core/profile.py`:
```python
@dataclass(frozen=True)
class TransportPorts:
    imu: int
    motor_fb: int
    motor_cmd: int
```

From `robot_platform/tools/platform_cli/main.py`:
```python
def _parse_verify_phase2_args(args: list[str]) -> tuple[str, Path, str | None]: ...
def _run_verify_phase2(project: str, report_path: Path, case_name: str | None) -> int: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 3 artifact-schema and runtime-output regression coverage</name>
  <files>
robot_platform/sim/tests/test_runner.py
robot_platform/tools/platform_cli/tests/test_main.py
  </files>
  <read_first>
robot_platform/sim/tests/test_runner.py
robot_platform/tools/platform_cli/tests/test_main.py
robot_platform/sim/core/runner.py
robot_platform/tools/platform_cli/main.py
robot_platform/sim/projects/balance_chassis/profile.py
robot_platform/sim/projects/balance_chassis/validation.py
robot_platform/runtime/app/balance_chassis/app_bringup/remote_task.c
robot_platform/runtime/control/state/observe_task.c
robot_platform/runtime/app/balance_chassis/app_bringup/chassis_task.c
robot_platform/runtime/control/execution/motor_control_task.c
  </read_first>
  <behavior>
    - Test 1: `verify phase3` writes cases `runtime_binding`, `runtime_outputs`, and `artifact_schema` with a machine-readable `overall_status`.
    - Test 2: smoke summaries expose `adapter_bindings`, `adapter_binding_summary`, and a count field derived from observed runtime output events.
  </behavior>
  <action>Create failing Python regressions before implementation. Per D-01 through D-04, keep the case names and expected JSON fields anchored to the thin application-layer proof surface rather than module names: the tests should prove the authoritative chain `remote input + state observation -> intent parsing / mode constraints -> chassis control -> execution output`, along with safety status and observability, rather than define a new architecture. In `robot_platform/tools/platform_cli/tests/test_main.py`, add `ParseVerifyPhase3ArgsTests` plus `VerifyPhase3Tests` that expect a default report path of `build/verification_reports/phase3_balance_chassis.json`, allow `--case runtime_binding`, `--case runtime_outputs`, and `--case artifact_schema`, and assert the resulting JSON includes a top-level `cases` array, a `stages` entry named `smoke`, and a `runtime_binding` case description that references the exact authoritative chain string. In `robot_platform/sim/tests/test_runner.py`, add assertions that the summarized smoke payload includes exact keys `adapter_bindings`, `adapter_binding_summary`, and `runtime_output_observation_count`.</action>
  <acceptance_criteria>
    - `robot_platform/tools/platform_cli/tests/test_main.py` contains `class ParseVerifyPhase3ArgsTests` and `class VerifyPhase3Tests`.
    - `robot_platform/tools/platform_cli/tests/test_main.py` contains the exact string `remote input + state observation -> intent parsing / mode constraints -> chassis control -> execution output`.
    - `robot_platform/sim/tests/test_runner.py` contains the exact strings `adapter_bindings`, `adapter_binding_summary`, and `runtime_output_observation_count`.
    - `rg -n "phase3" robot_platform/tools/platform_cli/tests/test_main.py` returns matches for `runtime_binding`, `runtime_outputs`, and `artifact_schema`.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest robot_platform.sim.tests.test_runner robot_platform.tools.platform_cli.tests.test_main -v</automated>
  </verify>
  <done>There are failing regression expectations for the Phase 3 artifact schema and the dedicated verification command before implementation starts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `verify phase3` and persist runtime-output plus adapter-binding evidence</name>
  <files>
robot_platform/runtime/app/balance_chassis/app_io/chassis_topics.c
robot_platform/sim/backends/sitl_bridge.py
robot_platform/sim/core/profile.py
robot_platform/sim/core/runner.py
robot_platform/sim/projects/balance_chassis/bridge_adapter.py
robot_platform/sim/projects/balance_chassis/profile.py
robot_platform/sim/projects/balance_chassis/validation.py
robot_platform/tools/platform_cli/main.py
  </files>
  <read_first>
robot_platform/runtime/app/balance_chassis/app_io/chassis_topics.c
robot_platform/sim/backends/sitl_bridge.py
robot_platform/sim/core/profile.py
robot_platform/sim/core/runner.py
robot_platform/sim/projects/balance_chassis/profile.py
robot_platform/sim/projects/balance_chassis/validation.py
robot_platform/tools/platform_cli/main.py
.planning/phases/03-fake-link-runtime-proof/03-RESEARCH.md
robot_platform/runtime/control/state/observe_task.c
robot_platform/runtime/app/balance_chassis/app_intent/remote_intent.c
robot_platform/runtime/app/balance_chassis/app_bringup/remote_task.c
robot_platform/runtime/app/balance_chassis/app_bringup/chassis_task.c
robot_platform/runtime/control/execution/motor_control_task.c
  </read_first>
  <behavior>
    - Test 1: `verify phase3 --case runtime_binding` passes only when fake-link inputs drive the authoritative chain and the artifact records that chain machine-readably.
    - Test 2: `verify phase3 --case runtime_outputs` fails if `runtime_output_observation_count` is zero and passes when `actuator_command` observations are present.
  </behavior>
  <action>Implement the authoritative Phase 3 proof surface around the existing smoke runner. LINK-01 ownership is now shared here with 03-01, and this task is the runtime-level closure for it: the `runtime_binding` case must prove fake-link inputs drive the authoritative chain `remote input + state observation -> intent parsing / mode constraints -> chassis control -> execution output`, not just adapter metadata. Per D-01 through D-03, keep the report vocabulary focused on the thin application-layer responsibilities already in scope rather than turning current file seams into architecture commitments. Extend `TransportPorts` to include a dedicated remote-input port and update `BALANCE_CHASSIS_PROFILE` with that declaration. In `sitl_bridge.py`, emit machine-readable `adapter_binding` events for `imu`, `remote`, and `motor` with exact fields `name`, `transport`, `bound`, and `port`; keep the bridge thin per D-07 and D-08 and do not synthesize control logic. Before changing artifact fields, read `remote_task.c`, `observe_task.c`, `remote_intent.c`, `chassis_task.c`, and `motor_control_task.c` so the report shape continues to treat state observation as a peer input to intent/mode reasoning per D-05 and explicitly describes the D-04 chain. In `chassis_topics.c`, enrich the first `[RuntimeOutput]` line to include `topic=actuator_command`, `sample_count`, `start`, `control_enable`, and `actuator_enable`. In `runner.py`, persist `adapter_bindings`, `adapter_binding_summary`, `runtime_output_observations`, and `runtime_output_observation_count`, and add a `runtime_binding` summary object that records whether the authoritative chain ran and includes the exact chain string. Teach `build_validation_status` to use observed runtime-output topics rather than declarations alone. In `main.py`, add `_parse_verify_phase3_args`, a `PHASE3_CASES` map, and `_run_verify_phase3` that writes one JSON artifact under `build/verification_reports/phase3_balance_chassis.json` with cases `runtime_binding`, `runtime_outputs`, and `artifact_schema`. Do not broaden this task into higher-fidelity simulator behavior or digital-twin work because D-09 keeps proof and observability as the priority.</action>
  <acceptance_criteria>
    - `robot_platform/tools/platform_cli/main.py` contains `def _parse_verify_phase3_args`, `PHASE3_CASES`, and `def _run_verify_phase3`.
    - `robot_platform/sim/backends/sitl_bridge.py` contains the exact string `adapter_binding`.
    - `robot_platform/tools/platform_cli/main.py` or `robot_platform/sim/core/runner.py` contains the exact string `remote input + state observation -> intent parsing / mode constraints -> chassis control -> execution output`.
    - `robot_platform/sim/core/runner.py` contains `runtime_binding`.
    - `robot_platform/sim/core/runner.py` contains `adapter_bindings`, `adapter_binding_summary`, and `runtime_output_observation_count`.
    - `robot_platform/sim/core/profile.py` contains a `remote:` field inside `TransportPorts`.
    - `robot_platform/runtime/app/balance_chassis/app_io/chassis_topics.c` contains `sample_count=` in the `[RuntimeOutput]` log line.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest robot_platform.sim.tests.test_runner robot_platform.tools.platform_cli.tests.test_main -v</automated>
  </verify>
  <done>`LINK-01`, `LINK-02`, and `OBS-01` are satisfied with a dedicated Phase 3 verification command and machine-readable artifacts containing both authoritative runtime-chain proof, runtime outputs, and adapter-binding status.</done>
</task>

</tasks>

<verification>
Run the Python regression modules after each task. Finish the plan by running `python3 -m robot_platform.tools.platform_cli.main verify phase3 --case runtime_outputs` once implementation lands.
</verification>

<success_criteria>
Phase 3 now has one authoritative JSON verification artifact that records adapter binding truth, authoritative runtime-chain proof for `runtime_binding`, runtime output observations, and per-case outcomes for `runtime_binding`, `runtime_outputs`, and `artifact_schema`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-fake-link-runtime-proof/03-02-SUMMARY.md`
</output>
