---
phase: v2-04-consolidation
plan: "03"
type: execute
wave: 3
depends_on: ["v2-04-01", "v2-04-02"]
files_modified:
  - robot_platform/CMakeLists.txt
autonomous: true

must_haves:
  truths:
    - "CMake interface libraries eliminate duplicated include paths and sanitizer config across 12+ test targets"
    - "No test target has its own -fsanitize compile/link options — all inherit from a shared interface library"
    - "No test target has its own -Wall -Wextra -Wpedantic block — all inherit from a shared interface library"
    - "validate pipeline runs all 5 stages to completion (build_sitl, host_tests, python_tests, smoke, verify_phase3)"
    - "All 12 CTest targets pass"
    - "HW ELF target still compiles (if cross-compiler available)"
  artifacts:
    - path: "robot_platform/CMakeLists.txt"
      provides: "Cleaned CMake with interface libraries for shared config"
      contains: "add_library.*INTERFACE"
  key_links:
    - from: "robot_platform/CMakeLists.txt"
      to: "test targets"
      via: "target_link_libraries with interface library"
      pattern: "target_link_libraries.*platform_host_test_config"
---

<objective>
Refactor CMakeLists.txt to use CMake interface libraries for shared include paths, compile options, and sanitizer config. Then run the full validate pipeline to confirm end-to-end integrity.

Purpose: Eliminate ~120 lines of duplicated CMake configuration across 12 test targets (QUAL-03). Currently every test target repeats the same `-fsanitize=address,undefined`, `-fno-omit-frame-pointer`, `-Wall -Wextra -Wpedantic -Wno-unused-parameter -O0 -g3` blocks and ASAN_OPTIONS environment. A single interface library makes this DRY. The validate pipeline run satisfies KEEP-02.

Output: CMakeLists.txt significantly shorter and cleaner, validate pipeline passes all 5 stages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v2-04-consolidation/v2-04-01-SUMMARY.md
@.planning/phases/v2-04-consolidation/v2-04-02-SUMMARY.md
@robot_platform/CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CMake interface libraries and refactor test targets</name>
  <files>
    robot_platform/CMakeLists.txt
  </files>
  <action>
    1. Create a `platform_host_test_config` INTERFACE library inside the `if(PLATFORM_HOST_TESTS)` block, right after `enable_testing()`:
       ```cmake
       add_library(platform_host_test_config INTERFACE)
       target_compile_features(platform_host_test_config INTERFACE c_std_11)
       target_compile_options(platform_host_test_config INTERFACE
         -Wall -Wextra -Wpedantic -Wno-unused-parameter -O0 -g3
       )
       if(PLATFORM_HOST_TEST_SANITIZERS AND CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
         target_compile_options(platform_host_test_config INTERFACE
           -fsanitize=address,undefined -fno-omit-frame-pointer
         )
         target_link_options(platform_host_test_config INTERFACE
           -fsanitize=address,undefined
         )
       endif()
       ```

    2. For each standalone test executable (test_message_center, test_actuator_gateway, test_balance_app_startup), refactor to:
       - Add `target_link_libraries(test_xxx PRIVATE platform_host_test_config)` (or append to existing link line)
       - Remove the per-target `target_compile_features`, `target_compile_options` (the -Wall/-O0/-g3 block), and the `if(PLATFORM_HOST_TEST_SANITIZERS)` compile/link options block
       - Keep target-specific `target_include_directories` — those are NOT duplicated

    3. For each test executable that links `balance_safety_host_runtime` (test_balance_safety_path, test_device_profile_safety_seams, test_device_profile_sitl_runtime_bindings, test_safety_mapping, test_safety_sensor_faults, test_safety_arming, test_safety_saturation, test_safety_wheel_leg, test_bsp_ports_compile), refactor to:
       - Add `platform_host_test_config` to the `target_link_libraries` line
       - Remove per-target `target_compile_features`, `target_compile_options`, and sanitizer blocks
       - These targets are very short after cleanup: just `add_executable`, `target_link_libraries`, `add_test`, `set_tests_properties`

    4. Keep `set_tests_properties(test_xxx PROPERTIES ENVIRONMENT "ASAN_OPTIONS=detect_leaks=0")` on each test — CMake doesn't support inheriting test properties from interface libraries, so this stays per-test. BUT: consider creating a helper function/macro if it reduces noise:
       ```cmake
       function(platform_add_host_test TEST_NAME)
         add_test(NAME ${TEST_NAME} COMMAND ${TEST_NAME})
         set_tests_properties(${TEST_NAME} PROPERTIES
           ENVIRONMENT "ASAN_OPTIONS=detect_leaks=0"
         )
       endfunction()
       ```
       Then replace each `add_test` + `set_tests_properties` pair with `platform_add_host_test(test_xxx)`.

    5. Also refactor `balance_safety_host_runtime` static library:
       - It already has its own sanitizer block — replace with `target_link_libraries(balance_safety_host_runtime PUBLIC platform_host_test_config)` (or PRIVATE if compile options shouldn't propagate — but since test executables link it, PUBLIC is cleaner)
       - Remove its per-target `-Wall -Wextra -Wpedantic -Wno-unused-parameter -O0 -g3` and sanitizer blocks
       - Keep its `target_include_directories` and `target_compile_definitions` — those are specific

    6. Similarly refactor `cmsis_dsp_host` if it has duplicated compile options.

    IMPORTANT: Do NOT change any include paths, source lists, or compile definitions. Only consolidate compile options, sanitizer config, and C standard settings.
  </action>
  <verify>
    Count sanitizer occurrences: `grep -c 'fsanitize=address' robot_platform/CMakeLists.txt` — should be 2-3 (only in the interface library definition), down from 26.
    Rebuild: `cmake -S robot_platform -B build/host_test -DPLATFORM_TARGET_HW=OFF -DPLATFORM_HOST_TESTS=ON && cmake --build build/host_test`
    Run tests: `cd build/host_test && ctest --output-on-failure` — all 12 pass.
  </verify>
  <done>
    CMakeLists.txt uses `platform_host_test_config` interface library. Sanitizer config appears only once (in the interface library). Per-test boilerplate reduced to add_executable + target_link_libraries + platform_add_host_test. All 12 CTest targets pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full validate pipeline and confirm end-to-end closure</name>
  <files>
    (no file modifications — validation only)
  </files>
  <action>
    1. Run the full validate pipeline:
       ```
       cd /home/xbd/workspace/codes/Astra_RM_Platform
       python3 -m robot_platform.tools.platform_cli.main validate
       ```

    2. Verify the closure report shows all 5 stages passed:
       - Stage 1: build_sitl — passed
       - Stage 2: host_tests — passed
       - Stage 3: python_tests — passed
       - Stage 4: smoke — passed
       - Stage 5: verify_phase3 — passed

    3. Read the closure report JSON at `build/closure_reports/closure_balance_chassis.json` and confirm `overall_status: "passed"`.

    4. If any stage fails, diagnose and fix. Common issues:
       - build_sitl: CMake path errors from Plan 02 directory move — fix GENERATED_RAW_DIR or SITL include paths
       - host_tests: Missing include or link error from Plan 01 topic consolidation — fix include paths
       - smoke: SITL binary crash — check that wait_ready gates and message_center calls are correct
       - verify_phase3: Phase 3 verification cases may reference old paths — update if needed

    5. If the validate pipeline references `verify_phase3` and Phase 3 hasn't actually been executed yet, the stage may fail on Phase 3-specific checks. Document this as expected — the validate pipeline structure itself is working (KEEP-02 satisfied), even if Phase 3 content checks fail because Phase 3 work hasn't been done yet.
  </action>
  <verify>
    `cat build/closure_reports/closure_balance_chassis.json | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['overall_status'])"` prints "passed"
    OR: document which stage failed and why (if Phase 3 prerequisite not met).
  </verify>
  <done>
    Validate pipeline runs all 5 stages. Either all pass (overall_status=passed) or any failure is documented with root cause traced to Phase 3 prerequisite (not Phase 4 regression). KEEP-02 satisfied — the pipeline structure works end-to-end.
  </done>
</task>

</tasks>

<verification>
1. `grep -c 'fsanitize=address' robot_platform/CMakeLists.txt` <= 4 (down from 26)
2. `grep -c 'platform_host_test_config' robot_platform/CMakeLists.txt` >= 10 (used by all test targets)
3. Validate pipeline: `python3 -m robot_platform.tools.platform_cli.main validate` exits 0
4. Closure report: `build/closure_reports/closure_balance_chassis.json` shows `overall_status: "passed"`
5. All 12 CTest targets pass
</verification>

<success_criteria>
- CMakeLists.txt has a `platform_host_test_config` INTERFACE library with shared compile options and sanitizer config
- Sanitizer duplication reduced from 26 occurrences to ~2-3
- `platform_add_host_test` function eliminates repeated add_test/set_tests_properties boilerplate
- Validate pipeline runs all 5 stages to completion
- All 12 CTest targets pass
- Overall CMakeLists.txt line count reduced by ~100+ lines
</success_criteria>

<output>
After completion, create `.planning/phases/v2-04-consolidation/v2-04-03-SUMMARY.md`
</output>
