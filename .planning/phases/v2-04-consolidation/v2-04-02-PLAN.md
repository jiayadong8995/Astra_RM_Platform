---
phase: v2-04-consolidation
plan: "02"
type: execute
wave: 2
depends_on: ["v2-04-01"]
files_modified:
  - robot_platform/runtime/bsp/boards/stm32h7_ctrl_board/generated/Inc/*
  - robot_platform/runtime/bsp/boards/stm32h7_ctrl_board/generated/Src/*
  - robot_platform/runtime/generated/
  - robot_platform/CMakeLists.txt
  - robot_platform/tools/platform_cli/main.py
  - robot_platform/tools/cubemx_backend/main.py
autonomous: true

must_haves:
  truths:
    - "runtime/generated/ directory no longer exists — all generated CubeMX output lives under bsp/boards/stm32h7_ctrl_board/generated/"
    - "runtime/ top-level directories are exactly: app, bsp, control, module, tests — 5 directories (4 layers + tests)"
    - "runtime/ directory nesting is max 2 levels deep (e.g. bsp/boards/stm32h7_ctrl_board, control/controllers, module/algorithm/PID)"
    - "device/ directory is gone (Phase 3 prerequisite — verify, do not delete)"
    - "All 12 CTest targets pass after path updates"
    - "SITL binary compiles and links"
  artifacts:
    - path: "robot_platform/runtime/bsp/boards/stm32h7_ctrl_board/generated/"
      provides: "CubeMX generated sources relocated under BSP board directory"
      contains: "main.c"
    - path: "robot_platform/CMakeLists.txt"
      provides: "Updated GENERATED_RAW_DIR path"
      contains: "bsp/boards/stm32h7_ctrl_board/generated"
  key_links:
    - from: "robot_platform/CMakeLists.txt"
      to: "robot_platform/runtime/bsp/boards/stm32h7_ctrl_board/generated/"
      via: "GENERATED_RAW_DIR variable"
      pattern: "GENERATED_RAW_DIR.*bsp/boards/stm32h7_ctrl_board/generated"
    - from: "robot_platform/tools/platform_cli/main.py"
      to: "robot_platform/runtime/bsp/boards/stm32h7_ctrl_board/generated/"
      via: "codegen output path"
      pattern: "bsp.*boards.*generated|stm32h7_ctrl_board.*generated"
---

<objective>
Move `runtime/generated/stm32h7_ctrl_board_raw/` into `bsp/boards/stm32h7_ctrl_board/generated/` and verify the runtime directory structure matches the target 4-layer architecture.

Purpose: Eliminate the standalone `runtime/generated/` directory (SLIM-05, QUAL-02). Generated CubeMX output belongs under the board's BSP directory — it's board-specific, not a separate runtime layer. This reduces runtime top-level directories from 6 to 5 (app, bsp, control, module, tests) and keeps nesting at max 2 levels.

Output: `runtime/generated/` deleted, files relocated under `bsp/boards/stm32h7_ctrl_board/generated/`, all CMake and Python tooling paths updated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v2-04-consolidation/v2-04-01-SUMMARY.md
@robot_platform/CMakeLists.txt
@robot_platform/tools/platform_cli/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate generated directory and update all path references</name>
  <files>
    robot_platform/runtime/bsp/boards/stm32h7_ctrl_board/generated/
    robot_platform/runtime/generated/
    robot_platform/CMakeLists.txt
    robot_platform/tools/platform_cli/main.py
  </files>
  <action>
    1. Move the generated directory:
       ```
       mv robot_platform/runtime/generated/stm32h7_ctrl_board_raw \
          robot_platform/runtime/bsp/boards/stm32h7_ctrl_board/generated
       ```
       Then delete the now-empty `robot_platform/runtime/generated/` directory (it may contain a README.md — delete that too).

    2. Update CMakeLists.txt `GENERATED_RAW_DIR`:
       - Change: `set(GENERATED_RAW_DIR ${PLATFORM_RUNTIME_DIR}/generated/stm32h7_ctrl_board_raw)`
       - To: `set(GENERATED_RAW_DIR ${PLATFORM_BOARD_BSP_DIR}/generated)`
       - Note: `PLATFORM_BOARD_BSP_DIR` is already set to `${PLATFORM_RUNTIME_DIR}/bsp/boards/stm32h7_ctrl_board`
       - All downstream references use `${GENERATED_RAW_DIR}` so they auto-resolve. No other CMake path changes needed for the move itself.

    3. Update `robot_platform/tools/platform_cli/main.py` — the `_generate_balance_chassis` function:
       - Change `out_dir` from `repo_root / "robot_platform" / "runtime" / "generated" / "stm32h7_ctrl_board_raw"`
       - To: `repo_root / "robot_platform" / "runtime" / "bsp" / "boards" / "stm32h7_ctrl_board" / "generated"`

    4. Check if `robot_platform/tools/cubemx_backend/main.py` references the old path — update if so.

    5. Search for any other references to `generated/stm32h7_ctrl_board_raw` or `runtime/generated` across the repo and update them (docs, configs, etc.).

    6. Verify runtime/ top-level directories are now: app, bsp, control, device, module, tests
       - Note: `device/` should already be empty/deleted by Phase 3. If it still exists, verify it's empty and delete it. If Phase 3 hasn't run yet, leave it — it's Phase 3's responsibility.
  </action>
  <verify>
    - `ls robot_platform/runtime/generated` should fail (directory gone)
    - `ls robot_platform/runtime/bsp/boards/stm32h7_ctrl_board/generated/Inc/main.h` should succeed
    - `grep -r "runtime/generated" robot_platform/` should return zero hits (excluding git history)
    - Rebuild host tests: `cmake -S robot_platform -B build/host_test -DPLATFORM_TARGET_HW=OFF -DPLATFORM_HOST_TESTS=ON && cmake --build build/host_test`
    - `cd build/host_test && ctest --output-on-failure` — all 12 pass
  </verify>
  <done>
    `runtime/generated/` no longer exists. All generated CubeMX files live at `runtime/bsp/boards/stm32h7_ctrl_board/generated/`. CMake and Python CLI paths updated. All 12 CTest targets pass. SITL build path resolves correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify directory structure matches target 4-layer architecture</name>
  <files>
    (verification only — no file modifications expected)
  </files>
  <action>
    1. List runtime/ top-level directories and confirm they are exactly: app, bsp, control, module, tests
       - If `device/` still exists and is non-empty, this is a Phase 3 incomplete signal — document it but do not block
       - If `device/` exists but is empty, delete it

    2. Measure max directory nesting depth under runtime/:
       ```
       find robot_platform/runtime -type d | awk -F/ '{print NF-3, $0}' | sort -rn | head -10
       ```
       Target: max 2 levels below runtime/ for first-party code directories (bsp/boards/stm32h7_ctrl_board is 3 levels but contains board-specific generated code — acceptable)

    3. Count runtime layers: bsp, control, app, module = 4 layers (tests is not a runtime layer)

    4. Verify the layer dependency direction is clean:
       - `grep -r '#include.*app/' robot_platform/runtime/control/` — control should not depend on app (except chassis_control_task.h -> chassis_topics.h for observation, which is a known coupling)
       - `grep -r '#include.*control/' robot_platform/runtime/bsp/` — bsp should not depend on control
  </action>
  <verify>
    `ls -d robot_platform/runtime/*/` shows exactly: app/, bsp/, control/, module/, tests/ (plus device/ only if Phase 3 incomplete)
    `find robot_platform/runtime -maxdepth 3 -type d | wc -l` is reasonable (no deep nesting)
  </verify>
  <done>
    Runtime directory structure verified: 4 runtime layers (bsp -> control -> app -> module) plus tests. generated/ absorbed into bsp/boards/. Max nesting depth documented. Any Phase 3 residue (device/) noted if present.
  </done>
</task>

</tasks>

<verification>
1. `test ! -d robot_platform/runtime/generated` — generated directory gone
2. `test -f robot_platform/runtime/bsp/boards/stm32h7_ctrl_board/generated/Inc/main.h` — files relocated
3. `grep -r "runtime/generated" robot_platform/ --include="*.py" --include="*.cmake" --include="CMakeLists.txt"` — zero hits
4. All 12 CTest targets pass
5. Runtime top-level dirs: app, bsp, control, module, tests
</verification>

<success_criteria>
- `runtime/generated/` directory deleted
- Generated files at `runtime/bsp/boards/stm32h7_ctrl_board/generated/{Inc,Src}/`
- CMakeLists.txt GENERATED_RAW_DIR points to new location
- Python CLI codegen output path updated
- Runtime layer count is 4: bsp, control, app, module
- Directory nesting max 2 levels for first-party code
- All 12 CTest targets pass
</success_criteria>

<output>
After completion, create `.planning/phases/v2-04-consolidation/v2-04-02-SUMMARY.md`
</output>
