---
phase: 05-default-closure-loop
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Running `python3 -m robot_platform.tools.platform_cli.main validate` end-to-end produces a closure artifact at build/closure_reports/closure_balance_chassis.json"
    - "The closure artifact records each stage with name, status, exit_code, and duration_s"
    - "The pipeline stops at the first failing stage if any stage fails"
    - "A successful run proves all validation gates passed before firmware output was attempted"
  artifacts:
    - path: "build/closure_reports/closure_balance_chassis.json"
      provides: "Machine-readable closure evidence"
      contains: "closure_version"
  key_links:
    - from: "robot_platform/tools/platform_cli/main.py"
      to: "build/closure_reports/closure_balance_chassis.json"
      via: "_run_validate writes closure artifact"
      pattern: "closure_reports"
---

<objective>
Run the validate command end-to-end against the real codebase and verify the closure artifact is correct.

Purpose: Prove the closure loop works on the actual balance_chassis project, not just in mocked unit tests.
Output: Verified closure artifact and human-confirmed pipeline behavior.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-default-closure-loop/05-RESEARCH.md
@.planning/phases/05-default-closure-loop/05-01-SUMMARY.md
@robot_platform/tools/platform_cli/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run validate command end-to-end and inspect closure artifact</name>
  <files></files>
  <action>
Run the full validate command against the real codebase. This must be run unsandboxed since the SITL smoke stage requires UDP sockets.

1. Run: `cd /home/xbd/workspace/codes/Astra_RM_Platform && python3 -m robot_platform.tools.platform_cli.main validate`

2. If the command completes (pass or fail), inspect the closure artifact:
   `cat build/closure_reports/closure_balance_chassis.json`

3. Verify the artifact structure:
   - Has `closure_version: 1`
   - Has `project: "balance_chassis"`
   - Has `overall_status` (either "passed" or "failed")
   - Has `stages` array where each entry has `name`, `status`, `exit_code`, `duration_s`
   - Has `timestamp` as ISO-8601 string
   - If failed: has `failure_stage` and `failure_reason` identifying which stage broke
   - If passed: has `hw_elf_attempted` and `hw_elf_status` fields

4. If the smoke stage is blocked by UDP restrictions (sandbox), re-run unsandboxed. The research notes this is a known environment constraint.

5. Record the outcome for the checkpoint.
  </action>
  <verify>
  `test -f build/closure_reports/closure_balance_chassis.json && python3 -c "import json; d=json.load(open('build/closure_reports/closure_balance_chassis.json')); assert d['closure_version']==1; assert 'stages' in d; assert 'timestamp' in d; print('artifact valid')"` succeeds from repo root.
  </verify>
  <done>
  The validate command ran end-to-end and produced a structurally valid closure artifact. The artifact records stage-level verdicts with timing.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  The full `validate` closure loop for `balance_chassis`: one command that sequences build_sitl -> host_tests -> python_tests -> smoke -> verify_phase3 -> (optional) build_hw_elf, stops on failure, and writes a machine-readable closure artifact to `build/closure_reports/closure_balance_chassis.json`.
  </what-built>
  <how-to-verify>
  1. Review the closure artifact at `build/closure_reports/closure_balance_chassis.json`
  2. Confirm it has: closure_version, project, overall_status, stages array, timestamp
  3. Confirm each stage entry has: name, status, exit_code, duration_s
  4. If overall_status is "failed", confirm failure_stage correctly identifies which stage broke
  5. If overall_status is "passed", confirm hw_elf_status shows whether firmware build was attempted
  6. Run `python3 -m robot_platform.tools.platform_cli.main validate` yourself if you want to see the pipeline in action
  7. Confirm the command prints a one-line summary and report path at the end
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Closure artifact exists at `build/closure_reports/closure_balance_chassis.json`
2. Artifact has correct schema (closure_version, project, overall_status, stages, timestamp)
3. Each stage has name, status, exit_code, duration_s
4. Pipeline stopped at first failure (if any) and did not run later stages
5. Human confirmed the artifact and pipeline behavior
</verification>

<success_criteria>
- End-to-end validate command produces a valid closure artifact
- The artifact proves which stages passed and which (if any) failed
- Human confirms the closure loop meets Phase 5 success criteria
- PIPE-01 satisfied: one command path for build + test + smoke + verify + firmware
- PIPE-02 satisfied: machine-readable results identify which stage failed
</success_criteria>

<output>
After completion, create `.planning/phases/05-default-closure-loop/05-02-SUMMARY.md`
</output>
