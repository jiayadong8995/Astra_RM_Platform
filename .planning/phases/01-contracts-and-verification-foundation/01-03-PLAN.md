---
phase: 01-contracts-and-verification-foundation
plan: "03"
type: execute
wave: 4
depends_on:
  - 01-04
files_modified:
  - robot_platform/CMakeLists.txt
  - robot_platform/runtime/control/execution/actuator_gateway.c
  - robot_platform/runtime/tests/host/test_actuator_gateway.c
  - robot_platform/runtime/tests/host/test_support/device_layer_stubs.c
  - robot_platform/runtime/tests/host/test_support/device_layer_stubs.h
autonomous: true
requirements:
  - HOST-01
  - HOST-04
must_haves:
  truths:
    - `actuator_gateway` can be exercised in host-native C tests without pulling in full `device_layer` bring-up.
    - Dispatch and feedback behavior are verified through public gateway entrypoints on the current `balance_chassis` seam.
    - Sanitizers run on the actuator seam tests by default.
  artifacts:
    - path: robot_platform/runtime/tests/host/test_actuator_gateway.c
      provides: First-wave host regression coverage for gateway init, feedback capture, and command dispatch.
    - path: robot_platform/runtime/tests/host/test_support/device_layer_stubs.c
      provides: Minimal fake symbol surface to record written device commands and seed feedback.
    - path: robot_platform/runtime/control/execution/actuator_gateway.c
      provides: Public seam implementation exercised by the host tests.
  key_links:
    - from: robot_platform/runtime/tests/host/test_actuator_gateway.c
      to: robot_platform/runtime/control/execution/actuator_gateway.c
      via: public gateway APIs
      pattern: platform_actuator_gateway_
    - from: robot_platform/runtime/control/execution/actuator_gateway.c
      to: robot_platform/runtime/tests/host/test_support/device_layer_stubs.c
      via: `platform_device_*` host stubs
      pattern: platform_device_write_default_command
---

<objective>
Prove the second first-wave host seam, `actuator_gateway`, with only the thin fake/stub surface Phase 1 actually needs.

Purpose: Add secondary seam confidence after the minimum live proof and transport hardening are already in place, without diluting the phase into full `device_layer` testing.
Output: Secondary host-native actuator seam tests that exercise init, feedback capture, and command dispatch under sanitizers after the minimum live proof is already established.
</objective>

<execution_context>
@/home/xbd/.codex/get-shit-done/workflows/execute-plan.md
@/home/xbd/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-contracts-and-verification-foundation/01-CONTEXT.md
@.planning/phases/01-contracts-and-verification-foundation/01-RESEARCH.md
@robot_platform/CMakeLists.txt
@robot_platform/runtime/control/execution/actuator_gateway.h
@robot_platform/runtime/control/execution/actuator_gateway.c
@robot_platform/runtime/device/device_layer.h
@robot_platform/runtime/control/contracts/actuator_command.h
@robot_platform/runtime/control/contracts/device_feedback.h

<interfaces>
Gateway entrypoints:
```c
void platform_actuator_gateway_init(void);
platform_device_result_t platform_actuator_gateway_capture_feedback(platform_device_feedback_t *feedback_msg);
void platform_actuator_gateway_dispatch_command(const platform_actuator_command_t *actuator_msg,
                                                uint32_t systick);
```

Thin fake surface this plan should keep small:
```c
platform_device_result_t platform_device_init_default_profile(void);
platform_device_result_t platform_device_read_default_feedback(platform_device_feedback_t *feedback);
platform_device_result_t platform_device_write_default_command(const platform_device_command_t *command);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the placeholder actuator test with failing gateway seam cases</name>
  <read_first>
    - robot_platform/runtime/control/execution/actuator_gateway.h
    - robot_platform/runtime/control/execution/actuator_gateway.c
    - robot_platform/runtime/device/device_layer.h
    - robot_platform/runtime/control/contracts/actuator_command.h
    - robot_platform/runtime/control/contracts/device_feedback.h
  </read_first>
  <files>robot_platform/CMakeLists.txt, robot_platform/runtime/tests/host/test_actuator_gateway.c, robot_platform/runtime/tests/host/test_support/device_layer_stubs.c, robot_platform/runtime/tests/host/test_support/device_layer_stubs.h</files>
  <behavior>
    - Test 1: `platform_actuator_gateway_init()` calls the default-profile init seam exactly once.
    - Test 2: `platform_actuator_gateway_capture_feedback()` forwards the backend result and copies the seeded feedback payload unchanged.
    - Test 3: `platform_actuator_gateway_dispatch_command()` maps the six contract motors into device commands with the expected IDs, kinds, targets, and validity gating.
    - Test 4: `actuator_enable=false` clears dispatch validity even when per-motor inputs are otherwise valid.
  </behavior>
  <action>Add `test_actuator_gateway` to the existing host-test build graph, then replace the placeholder actuator test with failing public-API regression cases. Extend the thin stub support only enough to seed one feedback payload, capture one written `platform_device_command_t`, and expose reset/introspection helpers for the tests. Do not grow this into a general-purpose fake `device_layer` runtime per D-03.</action>
  <verify>
    <automated>ctest --test-dir build/robot_platform_host_tests --output-on-failure -R test_actuator_gateway</automated>
  </verify>
  <acceptance_criteria>
    - The host tests fail before the seam support is complete and pass after it is implemented.
    - The stub layer stays limited to the three `platform_device_*` functions already used by `actuator_gateway`.
    - The tests verify observable behavior through public APIs instead of reaching into static internals.
  </acceptance_criteria>
  <done>The repository has an executable secondary actuator seam spec ready for implementation after the minimum live proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make `actuator_gateway` host-verifiable without widening scope to full `device_layer` coverage</name>
  <read_first>
    - robot_platform/runtime/control/execution/actuator_gateway.c
    - robot_platform/runtime/tests/host/test_actuator_gateway.c
    - .planning/codebase/CONCERNS.md
  </read_first>
  <files>robot_platform/runtime/control/execution/actuator_gateway.c, robot_platform/runtime/tests/host/test_support/device_layer_stubs.c, robot_platform/runtime/tests/host/test_support/device_layer_stubs.h</files>
  <action>Adjust the gateway seam or the thin test support as needed so the failing tests pass under the host harness. Preserve the production public API, keep `platform_map_contract_command()` private, and avoid introducing `device_layer` dependencies beyond the three stubbed seams. The target state is a sanitizer-clean host regression for `actuator_gateway` that proves Phase 1 can exercise this high-risk seam with the smallest viable fake surface.</action>
  <verify>
    <automated>ctest --test-dir build/robot_platform_host_tests --output-on-failure -R test_actuator_gateway</automated>
  </verify>
  <acceptance_criteria>
    - The host test passes with sanitizer-enabled binaries.
    - The seam remains lightweight and focused on `actuator_gateway`, not broader `device_layer` behavior.
    - The captured device command reflects the contract fields the runtime actually dispatches today.
  </acceptance_criteria>
  <done>`actuator_gateway` is covered as a secondary host seam after the minimum live proof is already established, without broadening Phase 1 scope.</done>
</task>

</tasks>

<verification>
Run `ctest --test-dir build/robot_platform_host_tests --output-on-failure -R test_actuator_gateway`. A green result proves the second prioritized host seam is part of the trustworthy Phase 1 loop.
</verification>

<success_criteria>
`actuator_gateway` becomes a secondary host-covered seam after the minimum live proof is already established, and it still does not require full `device_layer` adoption in Phase 1.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contracts-and-verification-foundation/01-03-SUMMARY.md`
</output>
