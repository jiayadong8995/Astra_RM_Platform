---
phase: 01-contracts-and-verification-foundation
plan: "05"
type: execute
wave: 4
depends_on:
  - 01-04
files_modified:
  - robot_platform/tools/platform_cli/main.py
  - robot_platform/tools/platform_cli/tests/test_main.py
  - robot_platform/tools/cubemx_backend/main.py
autonomous: true
requirements:
  - PIPE-03
must_haves:
  truths:
    - Hardware-trusting commands refuse to proceed when generated STM32 artifacts are stale relative to their source inputs.
    - Generated-artifact freshness is determined deterministically, not by ad hoc timestamps alone.
    - The new freshness gate extends the Phase 1 verification/reporting surface without blocking the minimum live proof from being established first.
  artifacts:
    - path: robot_platform/tools/cubemx_backend/main.py
      provides: Deterministic generated-artifact metadata/check support.
    - path: robot_platform/tools/platform_cli/main.py
      provides: CLI enforcement for freshness before hardware-trusting paths continue.
    - path: robot_platform/tools/platform_cli/tests/test_main.py
      provides: Regression coverage for stale or missing generated-artifact metadata behavior.
  key_links:
    - from: robot_platform/tools/platform_cli/main.py
      to: robot_platform/tools/cubemx_backend/main.py
      via: freshness manifest check before hardware-trusting stages
      pattern: freshness
---

<objective>
Add the hard generated-artifact freshness gate after the minimum live proof is already in place, so hardware-trusting paths become deterministic without occupying the shortest evidence path.

Purpose: Implement D-11 and D-12 and satisfy `PIPE-03` without blocking the minimum live SITL proof.
Output: Deterministic freshness metadata plus hard CLI refusal when generated assets are missing or stale.
</objective>

<execution_context>
@/home/xbd/.codex/get-shit-done/workflows/execute-plan.md
@/home/xbd/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-contracts-and-verification-foundation/01-CONTEXT.md
@.planning/phases/01-contracts-and-verification-foundation/01-RESEARCH.md
@robot_platform/tools/platform_cli/main.py
@robot_platform/tools/platform_cli/tests/test_main.py
@robot_platform/tools/cubemx_backend/main.py
@robot_platform/tools/platform_cli/README.md
@robot_platform/docs/generated_import_rules.md
@references/legacy/Astra_RM2025_Balance_legacy/Chassis/CtrlBoard-H7_IMU.ioc
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define deterministic generated-artifact freshness metadata and failing CLI tests</name>
  <read_first>
    - robot_platform/tools/cubemx_backend/main.py
    - robot_platform/tools/platform_cli/main.py
    - robot_platform/tools/platform_cli/tests/test_main.py
    - .planning/phases/01-contracts-and-verification-foundation/01-CONTEXT.md
  </read_first>
  <files>robot_platform/tools/platform_cli/tests/test_main.py</files>
  <action>Add failing regression tests that define the target behavior for generated-artifact freshness: successful code generation records deterministic source/output metadata, and hardware-trusting commands fail when that metadata is missing or stale relative to `references/legacy/Astra_RM2025_Balance_legacy/Chassis/CtrlBoard-H7_IMU.ioc`. The tests must name exact failure modes and stage labels rather than relying on generic CLI text.</action>
  <verify>
    <automated>python3 -m unittest robot_platform.tools.platform_cli.tests.test_main -v</automated>
  </verify>
  <acceptance_criteria>
    - The new tests fail before freshness metadata/check logic is implemented.
    - The failing cases distinguish `missing_metadata` from `stale_generated_artifacts`.
    - The tests reference the real current IOC source path, not the removed old path.
  </acceptance_criteria>
  <done>The repository has an executable spec for deterministic generated-artifact freshness behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement freshness metadata and hard refusal in hardware-trusting commands</name>
  <read_first>
    - robot_platform/tools/cubemx_backend/main.py
    - robot_platform/tools/platform_cli/main.py
    - robot_platform/tools/platform_cli/tests/test_main.py
    - robot_platform/docs/generated_import_rules.md
  </read_first>
  <files>robot_platform/tools/cubemx_backend/main.py, robot_platform/tools/platform_cli/main.py, robot_platform/tools/platform_cli/tests/test_main.py</files>
  <action>Add deterministic freshness metadata recording to the CubeMX generation flow and enforce a hard gate in hardware-trusting CLI paths. The target state is: after successful generation, the repo can compare the current IOC source identity and generated-output identity against recorded metadata; if metadata is missing or stale, firmware-producing or hardware-trusting commands stop immediately with a machine-readable failure instead of proceeding. Keep this implementation separate from the minimum live proof logic in `01-04` so it hardens the platform without lengthening the shortest evidence path.</action>
  <verify>
    <automated>python3 -m unittest robot_platform.tools.platform_cli.tests.test_main -v</automated>
  </verify>
  <acceptance_criteria>
    - Hardware-trusting commands refuse to proceed when metadata is missing or stale.
    - The real current IOC path is the freshness source of truth.
    - The failure is machine-readable and deterministic.
  </acceptance_criteria>
  <done>`PIPE-03` is satisfied without holding the minimum live proof hostage to freshness-gate work.</done>
</task>

</tasks>

<verification>
Run `python3 -m unittest robot_platform.tools.platform_cli.tests.test_main -v`. A green run proves generated-artifact freshness behavior is deterministic and enforced in CLI paths.
</verification>

<success_criteria>
Generated STM32 artifacts are treated as a hard trust boundary, but that hardening work no longer occupies the shortest critical path to proving the minimum live SITL chain.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contracts-and-verification-foundation/01-05-SUMMARY.md`
</output>
