---
phase: 01-contracts-and-verification-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - robot_platform/runtime/module/message_center/message_center.h
  - robot_platform/runtime/module/message_center/message_center.c
  - robot_platform/runtime/tests/host/test_message_center.c
autonomous: true
requirements:
  - HOST-01
  - HOST-04
  - ARCH-02
must_haves:
  truths:
    - `message_center` rejects unsafe topic registration instead of silently accepting overflow-prone payload sizing.
    - Large runtime contracts such as `platform_robot_state_t`, `platform_actuator_command_t`, `platform_device_feedback_t`, and `platform_device_input_t` can move through the message bus in host tests without fixed-64-byte corruption risk.
    - Sanitizer-backed host tests fail if transport storage or registration rules regress.
  artifacts:
    - path: robot_platform/runtime/module/message_center/message_center.h
      provides: Explicit declared-payload transport contract and failure behavior.
    - path: robot_platform/runtime/module/message_center/message_center.c
      provides: Static-memory implementation with per-topic payload sizing instead of fixed 64-byte subscriber buffers.
    - path: robot_platform/runtime/tests/host/test_message_center.c
      provides: Regression coverage for registration rejection, large-contract publish/read, size mismatch handling, and payload-size representation beyond 255 bytes.
  key_links:
    - from: robot_platform/runtime/tests/host/test_message_center.c
      to: robot_platform/runtime/module/message_center/message_center.c
      via: host executable linked in `test_message_center`
      pattern: PubRegister
    - from: robot_platform/runtime/module/message_center/message_center.c
      to: robot_platform/runtime/control/contracts/robot_state.h
      via: host tests asserting declared sizes larger than 64 bytes
      pattern: sizeof\(platform_robot_state_t\)
---

<objective>
Replace the unsafe fixed-size message payload assumption with explicit declared-size transport behavior that Phase 1 can trust.

Purpose: Implement D-05, D-06, and D-07 before the minimum live path is treated as trustworthy.
Output: Hardened `message_center` transport plus host regressions covering the real contract sizes on the `balance_chassis` path.
</objective>

<execution_context>
@/home/xbd/.codex/get-shit-done/workflows/execute-plan.md
@/home/xbd/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-contracts-and-verification-foundation/01-CONTEXT.md
@.planning/phases/01-contracts-and-verification-foundation/01-RESEARCH.md
@.planning/codebase/CONCERNS.md
@robot_platform/runtime/module/message_center/message_center.h
@robot_platform/runtime/module/message_center/message_center.c
@robot_platform/runtime/control/contracts/robot_state.h
@robot_platform/runtime/control/contracts/actuator_command.h
@robot_platform/runtime/control/contracts/device_feedback.h
@robot_platform/runtime/control/contracts/device_input.h

<interfaces>
Current `message_center` surface:
```c
Publisher_t *PubRegister(char *name, uint8_t data_len);
Subscriber_t *SubRegister(char *name, uint8_t data_len);
uint8_t PubPushMessage(Publisher_t *pub, void *data_ptr);
uint8_t SubGetMessage(Subscriber_t *sub, void *data_ptr);
```

Representative Phase 1 contract sizes called out by research:
```c
sizeof(platform_robot_state_t)
sizeof(platform_actuator_command_t)
sizeof(platform_device_feedback_t)
sizeof(platform_device_input_t)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the placeholder `message_center` test with failing contract-size and registration tests</name>
  <read_first>
    - robot_platform/runtime/module/message_center/message_center.h
    - robot_platform/runtime/module/message_center/message_center.c
    - robot_platform/runtime/control/contracts/robot_state.h
    - robot_platform/runtime/control/contracts/actuator_command.h
    - robot_platform/runtime/control/contracts/device_feedback.h
    - robot_platform/runtime/control/contracts/device_input.h
  </read_first>
  <files>robot_platform/runtime/tests/host/test_message_center.c</files>
  <behavior>
    - Test 1: duplicate topic registration with a mismatched declared size is rejected explicitly.
    - Test 2: subscriber registration with a mismatched declared size is rejected explicitly.
    - Test 3: `platform_robot_state_t`, `platform_actuator_command_t`, `platform_device_feedback_t`, and `platform_device_input_t` all register and publish/read successfully, including the current contract that exceeds 255 bytes.
    - Test 4: null publisher or null payload input does not silently report a successful publish.
  </behavior>
  <action>Replace the placeholder host test with real failing cases for D-05 through D-07. Use the actual runtime contract headers named in `01-CONTEXT.md` so the test suite proves the current `balance_chassis` data shapes, not synthetic toy structs. Explicitly include the current payload-size boundary above 255 bytes so the test suite forces the transport API and storage model to represent all known contracts, not just the ones that fit in the old `uint8_t` length type. Keep the test executable self-contained inside the Phase 1 host harness from Plan 01.</action>
  <verify>
    <automated>ctest --test-dir build/robot_platform_host_tests --output-on-failure -R test_message_center</automated>
  </verify>
  <acceptance_criteria>
    - The host test fails before implementation changes and passes after them.
    - The assertions cover both rejection behavior and large-contract success behavior, including one payload larger than 255 bytes.
    - The tests use the real runtime contract headers, not hand-copied stand-ins.
  </acceptance_criteria>
  <done>The repo has an executable transport-regression spec for the Phase 1 message contract hardening work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement declared-size topic storage and explicit registration failures</name>
  <read_first>
    - robot_platform/runtime/module/message_center/message_center.h
    - robot_platform/runtime/module/message_center/message_center.c
    - .planning/codebase/CONCERNS.md
    - .planning/phases/01-contracts-and-verification-foundation/01-RESEARCH.md
  </read_first>
  <files>robot_platform/runtime/module/message_center/message_center.h, robot_platform/runtime/module/message_center/message_center.c</files>
  <action>Replace the fixed `uint8_t data_buf[64]` subscriber storage design with static-memory transport state that respects each topic’s declared payload size. Preserve the no-malloc constraint, but make registration fail explicitly when the topic name is invalid, the declared size is zero, duplicate registrations disagree on size, or the static storage pool cannot satisfy the declared bytes. Widen the public size representation as needed so the API can represent all current runtime contracts, including `platform_device_input_t`, even if that requires changing the `uint8_t data_len` surface to a wider type. The target state is that the real large `balance_chassis` contracts move through the bus safely, and unsafe registrations fail at setup time per D-06 and D-07.</action>
  <verify>
    <automated>ctest --test-dir build/robot_platform_host_tests --output-on-failure -R test_message_center</automated>
  </verify>
  <acceptance_criteria>
    - `message_center` no longer relies on a global 64-byte payload ceiling or an 8-bit declared-size ceiling.
    - Registration failures happen deterministically during setup rather than through later memory corruption.
    - The host test remains sanitizer-clean when moving the large runtime contracts, including one larger than 255 bytes.
  </acceptance_criteria>
  <done>`message_center` is safe enough to serve as a trustworthy prerequisite for the Phase 1 live path.</done>
</task>

</tasks>

<verification>
Run `ctest --test-dir build/robot_platform_host_tests --output-on-failure -R test_message_center`. A green run proves both transport safety and sanitizer-backed coverage for the first high-risk seam.
</verification>

<success_criteria>
Phase 1 no longer depends on the overflow-prone 64-byte topic assumption, and the real runtime contracts are covered by host-native regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contracts-and-verification-foundation/01-02-SUMMARY.md`
</output>
