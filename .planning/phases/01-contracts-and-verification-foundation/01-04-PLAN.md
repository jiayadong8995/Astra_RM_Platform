---
phase: 01-contracts-and-verification-foundation
plan: "04"
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - robot_platform/sim/backends/sitl_bridge.py
  - robot_platform/sim/projects/balance_chassis/profile.py
  - robot_platform/sim/projects/balance_chassis/validation.py
  - robot_platform/sim/tests/test_runner.py
  - robot_platform/tools/platform_cli/main.py
  - robot_platform/tools/platform_cli/tests/test_main.py
autonomous: false
requirements:
  - PIPE-02
must_haves:
  truths:
    - The minimum live path can produce one passed JSON-first verification artifact for `balance_chassis`.
    - The smoke path requires one actually observed runtime output instead of declared-only placeholders.
    - Machine-readable verification output identifies the exact failing stage when build, host tests, or smoke fail.
  artifacts:
    - path: robot_platform/tools/platform_cli/main.py
      provides: Authoritative Phase 1 verification entrypoint and JSON report orchestration.
    - path: robot_platform/sim/backends/sitl_bridge.py
      provides: Runtime-output observation emission from the real SITL bridge path.
    - path: robot_platform/sim/projects/balance_chassis/profile.py
      provides: Minimum-smoke output contract aligned to one observable runtime topic.
  key_links:
    - from: robot_platform/tools/platform_cli/main.py
      to: robot_platform/sim/backends/sitl_bridge.py
      via: verification `smoke` stage invoking the SITL runner
      pattern: run_sitl_session|smoke
    - from: robot_platform/sim/backends/sitl_bridge.py
      to: robot_platform/sim/projects/balance_chassis/validation.py
      via: `runtime_output_observation` events for the required output topic
      pattern: runtime_output_observation
---

<objective>
Close the minimum live Phase 1 proof: build SITL, launch SITL, bring the bridge up, inject one input, observe one real runtime output, and emit one authoritative verification report.

Purpose: Implement D-08 through D-10 and D-13 through D-14 with one JSON-first verification path centered on the minimum live proof.
Output: A report-driven Phase 1 closure command plus smoke validation that no longer passes on declared-only outputs.
</objective>

<execution_context>
@/home/xbd/.codex/get-shit-done/workflows/execute-plan.md
@/home/xbd/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-contracts-and-verification-foundation/01-CONTEXT.md
@.planning/phases/01-contracts-and-verification-foundation/01-RESEARCH.md
@.planning/phases/01-contracts-and-verification-foundation/01-VALIDATION.md
@robot_platform/tools/platform_cli/main.py
@robot_platform/tools/platform_cli/tests/test_main.py
@robot_platform/sim/core/runner.py
@robot_platform/sim/backends/sitl_bridge.py
@robot_platform/sim/projects/balance_chassis/profile.py
@robot_platform/sim/projects/balance_chassis/validation.py
@robot_platform/sim/tests/test_runner.py

<interfaces>
Current CLI surface from `robot_platform/tools/platform_cli/main.py`:
```python
def _run_sim(project: str, scenario: str, *, duration_s: float = 3.0, skip_build: bool = False) -> int: ...
def _run_tests(scope: str) -> int: ...
def main() -> int: ...
```

Current bridge event channel from `robot_platform/sim/backends/sitl_bridge.py`:
```python
def emit_event(event_type: str, payload: dict[str, object]) -> None: ...
```

Current validation shape from `robot_platform/sim/projects/balance_chassis/validation.py`:
```python
def build_validation_status(
    summary: dict[str, object],
    profile: SimProjectProfile,
) -> list[ValidationStatus]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the smoke path require one real observed runtime output</name>
  <read_first>
    - robot_platform/sim/backends/sitl_bridge.py
    - robot_platform/sim/projects/balance_chassis/profile.py
    - robot_platform/sim/projects/balance_chassis/validation.py
    - robot_platform/sim/tests/test_runner.py
    - .planning/phases/01-contracts-and-verification-foundation/01-RESEARCH.md
  </read_first>
  <files>robot_platform/sim/backends/sitl_bridge.py, robot_platform/sim/projects/balance_chassis/profile.py, robot_platform/sim/projects/balance_chassis/validation.py, robot_platform/sim/tests/test_runner.py</files>
  <behavior>
    - Test 1: bridge metadata and validation treat `actuator_command` as the required Phase 1 runtime output target.
    - Test 2: receiving motor commands from the SITL runtime emits `runtime_output_observation` events tagged to that target.
    - Test 3: smoke validation stays failed or pending when no runtime output is observed.
  </behavior>
  <action>Replace the current declared-only output target set (`chassis_state`, `leg_left`, `leg_right`) with one output the existing runtime already emits and the bridge can actually observe: `actuator_command`. Update the balance-chassis sim profile and validation expectations accordingly, and teach the SITL bridge to emit runtime-output observations when the real motor-command traffic arrives from the runtime path. Do not fabricate synthetic `chassis_state` data, and do not broaden this into a multi-output observation system; the goal is the smallest trustworthy proof per D-13 and D-14.</action>
  <verify>
    <automated>python3 -m unittest robot_platform.sim.tests.test_runner -v</automated>
  </verify>
  <acceptance_criteria>
    - A required smoke target is observed only when real runtime-output traffic arrives.
    - The tests no longer assume declared-only `chassis_state` / `leg_left` / `leg_right` outputs are acceptable proof.
    - The observation path stays tied to the actual SITL bridge traffic, not a fake side channel.
  </acceptance_criteria>
  <done>The minimum live path has one concrete observable runtime output that later reporting can rely on.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the JSON-first verification command for the minimum live path</name>
  <read_first>
    - robot_platform/tools/platform_cli/main.py
    - robot_platform/tools/platform_cli/tests/test_main.py
    - .planning/phases/01-contracts-and-verification-foundation/01-CONTEXT.md
    - .planning/phases/01-contracts-and-verification-foundation/01-RESEARCH.md
  </read_first>
  <files>robot_platform/tools/platform_cli/main.py, robot_platform/tools/platform_cli/tests/test_main.py</files>
  <behavior>
    - Test 1: the authoritative Phase 1 verification command `python3 -m robot_platform.tools.platform_cli.main verify phase1 --project balance_chassis --report build/verification_reports/phase1_balance_chassis.json --smoke-duration 1.0` writes that exact JSON artifact with ordered stages and one overall pass/fail result.
    - Test 2: when a stage fails, the JSON identifies the failing stage name and failure reason.
  </behavior>
  <action>Extend the CLI with one authoritative Phase 1 verification entrypoint named `verify phase1`, invoked exactly as `python3 -m robot_platform.tools.platform_cli.main verify phase1 --project balance_chassis --report build/verification_reports/phase1_balance_chassis.json --smoke-duration 1.0`. This command is the prioritized D-13 closure surface and must run the minimum live path stages in order after the host-harness and `message_center` prerequisites from Plans 01 and 02; do not make `actuator_gateway` coverage from Plan 03 a prerequisite because it is secondary under D-14. The report at `build/verification_reports/phase1_balance_chassis.json` must include stage identity, status, exit code, duration, and failure reason, with human-readable stdout reduced to a summary derived from that artifact per D-08 through D-10. Do not expand this task into generated-artifact freshness enforcement; that belongs to a later non-critical-path plan.</action>
  <verify>
    <automated>python3 -m unittest robot_platform.tools.platform_cli.tests.test_main -v</automated>
  </verify>
  <acceptance_criteria>
    - One JSON artifact is the primary output of a verification run.
    - Stage failure attribution is machine-readable and stage-specific.
  </acceptance_criteria>
  <done>Phase 1 has one report-driven verification entrypoint for the minimum live path, not separate ad hoc status outputs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the full minimum live path in an environment that allows local UDP sockets</name>
  <read_first>
    - .planning/phases/01-contracts-and-verification-foundation/01-VALIDATION.md
    - robot_platform/tools/platform_cli/main.py
    - robot_platform/sim/projects/balance_chassis/profile.py
  </read_first>
  <files>none (verification-only checkpoint)</files>
  <action>After Tasks 1 and 2 are complete, run the authoritative Phase 1 verification command exactly as `python3 -m robot_platform.tools.platform_cli.main verify phase1 --project balance_chassis --report build/verification_reports/phase1_balance_chassis.json --smoke-duration 1.0` in the intended development environment, not the restricted sandbox that previously failed UDP socket startup. The goal is to prove the exact path locked in D-13: build SITL, launch SITL, bridge up, inject one input, observe one runtime output, and emit a passed JSON report. If UDP socket creation or bind fails in the restricted environment, do not retry inside the same sandbox and do not continue to downstream interpretation as if smoke had run; the command must stop after writing `build/verification_reports/phase1_balance_chassis.json` with `overall_status: "blocked"`, `failure_stage: "smoke"`, `failure_reason` containing the socket/bind error, `stages` entries for each attempted stage, and the smoke stage marked `status: "blocked"` with `observed_outputs: []`. The fallback outcome is a recorded blocked result that is then rerun outside the restricted sandbox or on a host environment with UDP available.</action>
  <verify>
    <manual>1. Run `python3 -m robot_platform.tools.platform_cli.main verify phase1 --project balance_chassis --report build/verification_reports/phase1_balance_chassis.json --smoke-duration 1.0`. 2. Open `build/verification_reports/phase1_balance_chassis.json`. 3. If UDP is available, confirm `overall_status` is `passed`, the ordered stages include `smoke`, and the `smoke` stage records at least one observed `actuator_command` runtime output. 4. If UDP is unavailable, confirm the artifact still exists and records `overall_status: "blocked"`, `failure_stage: "smoke"`, a concrete socket/bind `failure_reason`, and `observed_outputs: []`, then rerun the same command outside the restricted sandbox before resuming.</manual>
  </verify>
  <acceptance_criteria>
    - The live environment produces one passed JSON artifact for the full minimum path.
    - The artifact proves at least one real runtime output was observed.
    - If UDP is unavailable, the command produces a blocked JSON artifact with the required fields instead of silently failing or retrying indefinitely inside the sandbox.
    - Any failure is attributable from the JSON without relying on ad hoc stdout interpretation.
  </acceptance_criteria>
  <done>The minimum live Phase 1 proof is confirmed in an environment where the SITL bridge can actually bind its sockets.</done>
</task>

</tasks>

<verification>
Automated: `python3 -m unittest robot_platform.sim.tests.test_runner -v` and `python3 -m unittest robot_platform.tools.platform_cli.tests.test_main -v`.
Manual checkpoint: run `python3 -m robot_platform.tools.platform_cli.main verify phase1 --project balance_chassis --report build/verification_reports/phase1_balance_chassis.json --smoke-duration 1.0` in a non-restricted environment and inspect `build/verification_reports/phase1_balance_chassis.json` for either a passed smoke stage with one observed `actuator_command` or a blocked smoke result that must be rerun outside the sandbox.
</verification>

<success_criteria>
Phase 1 can produce one authoritative JSON verification artifact for the minimum live path.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contracts-and-verification-foundation/01-04-SUMMARY.md`
</output>
