---
phase: 01-contracts-and-verification-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - robot_platform/CMakeLists.txt
  - robot_platform/runtime/tests/host/test_message_center.c
autonomous: true
requirements:
  - HOST-01
  - HOST-04
must_haves:
  truths:
    - Developer can configure and run Linux host C tests from the repo build graph without STM32 hardware.
    - The first host-test executable runs with ASan and UBSan enabled in the default Phase 1 host path.
    - The initial host harness only proves the smallest viable entrypoint and does not widen into secondary seams yet.
  artifacts:
    - path: robot_platform/CMakeLists.txt
      provides: Linux host-test configure/build/CTest entrypoints with sanitizer flags.
    - path: robot_platform/runtime/tests/host/test_message_center.c
      provides: First-wave host test executable entry for transport-contract coverage.
  key_links:
    - from: robot_platform/CMakeLists.txt
      to: robot_platform/runtime/tests/host/test_message_center.c
      via: add_executable/add_test registration
      pattern: test_message_center
---

<objective>
Stand up the smallest viable host-native C verification loop that later Phase 1 plans can extend without reworking the build graph.

Purpose: Implement D-01, D-02, and D-03 by creating a lightweight host test foundation before any broader fake-link or `device_layer` work.
Output: One dedicated host-test build target, sanitizer defaults, and a stable configure/build/CTest path that later plans can extend.
</objective>

<execution_context>
@/home/xbd/.codex/get-shit-done/workflows/execute-plan.md
@/home/xbd/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-contracts-and-verification-foundation/01-CONTEXT.md
@.planning/phases/01-contracts-and-verification-foundation/01-RESEARCH.md
@.planning/phases/01-contracts-and-verification-foundation/01-VALIDATION.md
@.planning/codebase/TESTING.md
@robot_platform/CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the dedicated host-test build and sanitizer entrypoint</name>
  <read_first>
    - robot_platform/CMakeLists.txt
    - .planning/phases/01-contracts-and-verification-foundation/01-CONTEXT.md
    - .planning/codebase/TESTING.md
  </read_first>
  <files>robot_platform/CMakeLists.txt</files>
  <action>Add a Linux-only Phase 1 host-test path in `robot_platform/CMakeLists.txt` with explicit options for `PLATFORM_HOST_TESTS` and `PLATFORM_HOST_TEST_SANITIZERS`, default sanitizer flags for GCC/Clang host builds, and a `ctest` registration for `test_message_center`. Keep the existing STM32 and SITL targets unchanged, and make the canonical configure/build location `build/robot_platform_host_tests` so later plans can rely on the same path. Do not pull `actuator_gateway` or `device_layer` seams into this first infrastructure step.</action>
  <verify>
    <automated>cmake -S robot_platform -B build/robot_platform_host_tests -G "Unix Makefiles" -DCMAKE_TOOLCHAIN_FILE=robot_platform/cmake/toolchains/linux-gcc.cmake -DPLATFORM_TARGET_HW=OFF -DPLATFORM_TARGET_SIM=OFF -DPLATFORM_HOST_TESTS=ON -DPLATFORM_HOST_TEST_SANITIZERS=ON &amp;&amp; cmake --build build/robot_platform_host_tests --target test_message_center -j4</automated>
  </verify>
  <acceptance_criteria>
    - `build/robot_platform_host_tests` configures on Linux without pulling STM32-only toolchain requirements.
    - `test_message_center` exists as a CMake target.
    - Sanitizer flags are applied on the host-test path by default per HOST-04.
  </acceptance_criteria>
  <done>The repo has one stable host-test entrypoint that later plans can extend without redesigning the build graph.</done>
</task>

<task type="auto">
  <name>Task 2: Create the first minimal `message_center` host test file</name>
  <read_first>
    - robot_platform/runtime/module/message_center/message_center.h
    - .planning/phases/01-contracts-and-verification-foundation/01-CONTEXT.md
  </read_first>
  <files>robot_platform/runtime/tests/host/test_message_center.c</files>
  <action>Create the first minimal host test source file for `message_center` so the new harness compiles and executes through `ctest`. Keep the assertions intentionally lightweight in this bootstrap plan and leave real transport behavior checks to Plan 02. Do not introduce `actuator_gateway`, `device_layer`, or generic fake support in this task.</action>
  <verify>
    <automated>ctest --test-dir build/robot_platform_host_tests --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - The placeholder `message_center` host test compiles and executes through `ctest`.
    - No new host-test dependency drags in `actuator_gateway`, `device_layer`, or other secondary seams.
    - The harness is ready for Plan 02 to replace the placeholder test with real failing transport cases.
  </acceptance_criteria>
  <done>The Phase 1 host harness is live and ready for focused `message_center` hardening work.</done>
</task>

</tasks>

<verification>
Configure `build/robot_platform_host_tests`, build `test_message_center`, then run `ctest --test-dir build/robot_platform_host_tests --output-on-failure`. A passing run proves the smallest viable host loop exists before deeper Phase 1 work begins.
</verification>

<success_criteria>
The repository contains one lightweight, sanitizer-enabled host-test path that can execute a `message_center` test without hardware and without a broad fake ecosystem.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contracts-and-verification-foundation/01-01-SUMMARY.md`
</output>
