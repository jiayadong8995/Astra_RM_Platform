---
phase: 01-contracts-and-verification-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - robot_platform/CMakeLists.txt
  - robot_platform/runtime/tests/host/test_message_center.c
  - robot_platform/runtime/tests/host/test_actuator_gateway.c
  - robot_platform/runtime/tests/host/test_support/device_layer_stubs.c
  - robot_platform/runtime/tests/host/test_support/device_layer_stubs.h
autonomous: true
requirements:
  - HOST-01
  - HOST-04
must_haves:
  truths:
    - Developer can configure and run Linux host C tests from the repo build graph without STM32 hardware.
    - Host test executables run with ASan and UBSan enabled in the default Phase 1 host path.
    - The first-wave seam tests depend only on thin host stubs, not on full `device_layer` bring-up.
  artifacts:
    - path: robot_platform/CMakeLists.txt
      provides: Linux host-test configure/build/CTest entrypoints with sanitizer flags.
    - path: robot_platform/runtime/tests/host/test_message_center.c
      provides: First-wave host test executable entry for transport-contract coverage.
    - path: robot_platform/runtime/tests/host/test_actuator_gateway.c
      provides: First-wave host test executable entry for actuator seam coverage.
    - path: robot_platform/runtime/tests/host/test_support/device_layer_stubs.c
      provides: Thin fake device-layer symbols needed by actuator host tests.
  key_links:
    - from: robot_platform/CMakeLists.txt
      to: robot_platform/runtime/tests/host/test_message_center.c
      via: add_executable/add_test registration
      pattern: test_message_center
    - from: robot_platform/CMakeLists.txt
      to: robot_platform/runtime/tests/host/test_actuator_gateway.c
      via: add_executable/add_test registration
      pattern: test_actuator_gateway
    - from: robot_platform/runtime/tests/host/test_actuator_gateway.c
      to: robot_platform/runtime/tests/host/test_support/device_layer_stubs.c
      via: linker-resolved `platform_device_*` symbols
      pattern: platform_device_
---

<objective>
Stand up the smallest viable host-native C verification loop that later Phase 1 plans can extend without reworking the build graph.

Purpose: Implement D-01, D-02, and D-03 by creating a lightweight host test foundation before any broader fake-link or `device_layer` work.
Output: Dedicated host-test build targets, sanitizer defaults, and placeholder seam executables ready for `message_center` and `actuator_gateway`.
</objective>

<execution_context>
@/home/xbd/.codex/get-shit-done/workflows/execute-plan.md
@/home/xbd/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-contracts-and-verification-foundation/01-CONTEXT.md
@.planning/phases/01-contracts-and-verification-foundation/01-RESEARCH.md
@.planning/phases/01-contracts-and-verification-foundation/01-VALIDATION.md
@.planning/codebase/TESTING.md
@robot_platform/CMakeLists.txt
@robot_platform/runtime/control/execution/actuator_gateway.h
@robot_platform/runtime/device/device_layer.h

<interfaces>
From `robot_platform/runtime/control/execution/actuator_gateway.h`:
```c
void platform_actuator_gateway_init(void);
platform_device_result_t platform_actuator_gateway_capture_feedback(platform_device_feedback_t *feedback_msg);
void platform_actuator_gateway_dispatch_command(const platform_actuator_command_t *actuator_msg,
                                                uint32_t systick);
```

From `robot_platform/runtime/device/device_layer.h`:
```c
platform_device_result_t platform_device_init_default_profile(void);
platform_device_result_t platform_device_read_default_feedback(platform_device_feedback_t *feedback);
platform_device_result_t platform_device_write_default_command(const platform_device_command_t *command);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the dedicated host-test build and sanitizer entrypoints</name>
  <read_first>
    - robot_platform/CMakeLists.txt
    - .planning/phases/01-contracts-and-verification-foundation/01-CONTEXT.md
    - .planning/codebase/TESTING.md
  </read_first>
  <files>robot_platform/CMakeLists.txt</files>
  <action>Add a Linux-only Phase 1 host-test path in `robot_platform/CMakeLists.txt` with explicit options for `PLATFORM_HOST_TESTS` and `PLATFORM_HOST_TEST_SANITIZERS`, default sanitizer flags for GCC/Clang host builds, and `ctest` registrations for `test_message_center` and `test_actuator_gateway`. Keep the existing STM32 and SITL targets unchanged, and make the canonical configure/build location `build/robot_platform_host_tests` so later plans can rely on the same path.</action>
  <verify>
    <automated>cmake -S robot_platform -B build/robot_platform_host_tests -G "Unix Makefiles" -DCMAKE_TOOLCHAIN_FILE=robot_platform/cmake/toolchains/linux-gcc.cmake -DPLATFORM_TARGET_HW=OFF -DPLATFORM_TARGET_SIM=OFF -DPLATFORM_HOST_TESTS=ON -DPLATFORM_HOST_TEST_SANITIZERS=ON &amp;&amp; cmake --build build/robot_platform_host_tests --target test_message_center test_actuator_gateway -j4</automated>
  </verify>
  <acceptance_criteria>
    - `build/robot_platform_host_tests` configures on Linux without pulling STM32-only toolchain requirements.
    - Both first-wave test executables exist as CMake targets.
    - Sanitizer flags are applied on the host-test path by default per HOST-04.
  </acceptance_criteria>
  <done>The repo has one stable host-test entrypoint that later plans can extend without redesigning the build graph.</done>
</task>

<task type="auto">
  <name>Task 2: Create thin first-wave test files and device stubs</name>
  <read_first>
    - robot_platform/runtime/control/execution/actuator_gateway.h
    - robot_platform/runtime/device/device_layer.h
    - .planning/phases/01-contracts-and-verification-foundation/01-CONTEXT.md
  </read_first>
  <files>robot_platform/runtime/tests/host/test_message_center.c, robot_platform/runtime/tests/host/test_actuator_gateway.c, robot_platform/runtime/tests/host/test_support/device_layer_stubs.c, robot_platform/runtime/tests/host/test_support/device_layer_stubs.h</files>
  <action>Create the first-wave host test source files and a thin `test_support` shim that exports only the `platform_device_*` symbols required to link `actuator_gateway` tests. The initial files should compile and pass with minimal placeholder assertions, clearly separated so Plan 02 can replace the `message_center` placeholder with real transport tests and Plan 03 can replace the `actuator_gateway` placeholder with real seam tests. Do not create a generic fake framework, and do not pull `device_layer.c` into this host path per D-03 and D-04.</action>
  <verify>
    <automated>ctest --test-dir build/robot_platform_host_tests --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - The placeholder host tests compile and execute through `ctest`.
    - The stub layer exports only the small symbol surface needed by `actuator_gateway`.
    - No new host-test dependency drags in the full runtime `device_layer` implementation.
  </acceptance_criteria>
  <done>The Phase 1 host harness is live and ready for focused `message_center` and `actuator_gateway` hardening work.</done>
</task>

</tasks>

<verification>
Configure `build/robot_platform_host_tests`, build both seam executables, then run `ctest --test-dir build/robot_platform_host_tests --output-on-failure`. A passing run proves the smallest viable host loop exists before deeper Phase 1 work begins.
</verification>

<success_criteria>
The repository contains one lightweight, sanitizer-enabled host-test path that can execute `message_center` and `actuator_gateway` tests without hardware and without a broad fake ecosystem.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contracts-and-verification-foundation/01-01-SUMMARY.md`
</output>
