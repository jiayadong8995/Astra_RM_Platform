---
phase: 06-v1-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - robot_platform/runtime/bsp/sitl/main_sitl.c
  - robot_platform/runtime/control/control_config/control_task_params.h
  - robot_platform/runtime/control/task_registry/control_task_registry.c
  - robot_platform/tools/platform_cli/main.py
  - robot_platform/tools/platform_cli/tests/test_main.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "validate pipeline host_tests stage runs ALL host C test targets, not only Phase 2 safety tests"
    - "control_task_registry.c no longer includes app_params.h directly"
    - "SITL main_sitl.c printf calls produce real newlines in output"
  artifacts:
    - path: "robot_platform/tools/platform_cli/main.py"
      provides: "Widened validate host_tests stage covering all CTest targets"
      contains: "test_message_center|test_actuator_gateway|test_device_profile|test_balance_app_startup"
    - path: "robot_platform/runtime/control/control_config/control_task_params.h"
      provides: "Neutral control-task timing/priority defines for INS, chassis, motor_control, observe tasks"
      contains: "CONTROL_INS_TASK_PRIORITY|CONTROL_CHASSIS_TASK_PRIORITY"
    - path: "robot_platform/runtime/control/task_registry/control_task_registry.c"
      provides: "Task registration using control_task_params.h instead of app_params.h"
      contains: "control_task_params.h"
    - path: "robot_platform/runtime/bsp/sitl/main_sitl.c"
      provides: "Printf calls with real newlines"
  key_links:
    - from: "robot_platform/tools/platform_cli/main.py"
      to: "robot_platform/CMakeLists.txt"
      via: "CTest target names and regex patterns"
      pattern: "test_message_center|test_actuator_gateway|test_balance_app_startup|test_device_profile"
    - from: "robot_platform/runtime/control/task_registry/control_task_registry.c"
      to: "robot_platform/runtime/control/control_config/control_task_params.h"
      via: "include directive"
      pattern: '#include.*control_task_params\.h'
    - from: "robot_platform/runtime/control/control_config/control_task_params.h"
      to: "robot_platform/runtime/app/balance_chassis/app_config/app_params.h"
      via: "re-export of values (still derives from balance_chassis proving path)"
      pattern: "app_params.h"
---

<objective>
Close three code-level tech debt items from the v1 milestone audit: widen the validate pipeline's host_tests stage to cover all host C test targets, decouple control_task_registry.c from direct app_params.h inclusion, and fix SITL printf newline escaping.

Purpose: Ensure the validate pipeline is a comprehensive gate (not just Phase 2 safety tests), reduce app-to-control coupling, and fix SITL log parsing reliability.
Output: Updated main.py, control_task_params.h, control_task_registry.c, main_sitl.c, and test_main.py.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1-MILESTONE-AUDIT.md
@robot_platform/tools/platform_cli/main.py
@robot_platform/tools/platform_cli/tests/test_main.py
@robot_platform/runtime/control/task_registry/control_task_registry.c
@robot_platform/runtime/control/control_config/control_task_params.h
@robot_platform/runtime/app/balance_chassis/app_config/app_params.h
@robot_platform/runtime/bsp/sitl/main_sitl.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SITL newlines and decouple control_task_registry</name>
  <files>
    robot_platform/runtime/bsp/sitl/main_sitl.c
    robot_platform/runtime/control/control_config/control_task_params.h
    robot_platform/runtime/control/task_registry/control_task_registry.c
  </files>
  <action>
    1. In `main_sitl.c`: Replace all escaped `\\n` sequences with real `\n` in printf calls. There are 5 printf statements (lines 8, 14, 18, 22, 28, 39) that use `\\n` — change each to `\n`. The file is a C source file, so `\n` in a string literal is already a real newline. The current source has double-backslash which produces a literal backslash-n in output instead of a newline character.

    2. In `control_task_params.h`: Replace the current passthrough `#include "../../app/balance_chassis/app_config/app_params.h"` with direct `#define` statements for the 8 macros that `control_task_registry.c` needs:
       - `CONTROL_INS_TASK_STACK_BYTES 2048U`
       - `CONTROL_CHASSIS_TASK_STACK_BYTES 4096U`
       - `CONTROL_MOTOR_CONTROL_STACK_BYTES 2048U`
       - `CONTROL_OBSERVE_TASK_STACK_BYTES 2048U`
       - `CONTROL_INS_TASK_PRIORITY osPriorityRealtime`
       - `CONTROL_CHASSIS_TASK_PRIORITY osPriorityAboveNormal`
       - `CONTROL_MOTOR_CONTROL_TASK_PRIORITY osPriorityAboveNormal`
       - `CONTROL_OBSERVE_TASK_PRIORITY osPriorityHigh`
       Keep the comment explaining these values are grounded in the balance_chassis proving path. Include `cmsis_os.h` for the osPriority constants. Use `CONTROL_` prefix (not `APP_`) to signal control-layer ownership.
       Also keep the task period defines that other control code may use:
       - `CONTROL_INS_TASK_PERIOD_MS 1U`
       - `CONTROL_CHASSIS_TASK_PERIOD_MS 2U`
       - `CONTROL_MOTOR_CONTROL_TASK_PERIOD_MS 1U`
       - `CONTROL_OBSERVE_TASK_PERIOD_MS 3U`
       - `CONTROL_CHASSIS_STARTUP_DELAY_MS 6U`

    3. In `control_task_registry.c`: Replace `#include "../../app/balance_chassis/app_config/app_params.h"` with `#include "../control_config/control_task_params.h"`. Update the 4 osThreadDef calls to use the new `CONTROL_` prefixed macro names instead of `APP_` prefixed names:
       - `APP_INS_TASK_PRIORITY` -> `CONTROL_INS_TASK_PRIORITY`, `APP_INS_TASK_STACK_BYTES` -> `CONTROL_INS_TASK_STACK_BYTES`
       - `APP_CHASSIS_TASK_PRIORITY` -> `CONTROL_CHASSIS_TASK_PRIORITY`, `APP_CHASSIS_TASK_STACK_BYTES` -> `CONTROL_CHASSIS_TASK_STACK_BYTES`
       - `APP_MOTOR_CONTROL_TASK_PRIORITY` -> `CONTROL_MOTOR_CONTROL_TASK_PRIORITY`, `APP_MOTOR_CONTROL_STACK_BYTES` -> `CONTROL_MOTOR_CONTROL_STACK_BYTES`
       - `APP_OBSERVE_TASK_PRIORITY` -> `CONTROL_OBSERVE_TASK_PRIORITY`, `APP_OBSERVE_TASK_STACK_BYTES` -> `CONTROL_OBSERVE_TASK_STACK_BYTES`
  </action>
  <verify>
    - `grep -r 'app_params.h' robot_platform/runtime/control/` returns NO matches (only control_task_params.h should exist, and it should NOT include app_params.h)
    - `grep '\\\\n' robot_platform/runtime/bsp/sitl/main_sitl.c` returns NO matches (no escaped newlines remain)
    - SITL build still compiles: `python3 -m robot_platform.tools.platform_cli.main build sitl`
  </verify>
  <done>
    control_task_registry.c includes control_task_params.h (not app_params.h). control_task_params.h defines CONTROL_-prefixed task params directly without re-including app_params.h. main_sitl.c printf calls use real \n newlines. SITL binary compiles successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Widen validate pipeline host_tests to all CTest targets</name>
  <files>
    robot_platform/tools/platform_cli/main.py
    robot_platform/tools/platform_cli/tests/test_main.py
  </files>
  <action>
    1. In `main.py`, in the `_run_validate` function (around line 953), replace the current host_tests stage that only runs PHASE2_CASES targets with a widened version that runs ALL host C test targets. The complete list of CTest targets from CMakeLists.txt is:
       - Phase 1: `test_message_center`, `test_actuator_gateway`
       - Phase 2: `test_safety_mapping`, `test_safety_sensor_faults`, `test_safety_arming`, `test_safety_saturation`, `test_balance_safety_path`, `test_safety_wheel_leg`
       - Phase 3: `test_device_profile_sitl_runtime_bindings`, `test_device_profile_safety_seams`
       - Phase 4: `test_balance_app_startup`

       Add a module-level constant `ALL_HOST_TEST_TARGETS` (list of all 11 target names) and `ALL_HOST_TEST_REGEX` (joined with `|`). Replace the Stage 2 block in `_run_validate` to use these instead of deriving from PHASE2_CASES. Update the comment from "all PHASE2_CASES host C safety tests" to "all host C test targets".

    2. In `test_main.py`, update the `ValidateTests` class:
       - The `test_early_exit_on_host_tests_failure` test mocks `_run_host_ctest` — this should still work since the function signature is unchanged. No change needed there.
       - Add a new test `test_host_tests_stage_runs_all_targets` that verifies the validate pipeline passes ALL_HOST_TEST_TARGETS to `_run_host_ctest` (not just PHASE2_CASES targets). Use `mock.patch` on `_run_host_ctest` and inspect the call args to confirm the targets list includes `test_message_center`, `test_actuator_gateway`, `test_device_profile_sitl_runtime_bindings`, `test_device_profile_safety_seams`, and `test_balance_app_startup` in addition to the Phase 2 targets.
  </action>
  <verify>
    - `python3 -m pytest robot_platform/tools/platform_cli/tests/test_main.py -v` (or `python3 -m unittest robot_platform.tools.platform_cli.tests.test_main -v`) passes all tests including the new one
    - `grep 'ALL_HOST_TEST_TARGETS\|test_message_center\|test_actuator_gateway\|test_balance_app_startup\|test_device_profile' robot_platform/tools/platform_cli/main.py` shows the widened target list in the validate function
  </verify>
  <done>
    The validate pipeline's host_tests stage builds and runs all 11 host C test targets across Phases 1-4, not only the 6 Phase 2 safety tests. A new unit test confirms the widened target list is passed to _run_host_ctest.
  </done>
</task>

</tasks>

<verification>
1. SITL build compiles: `python3 -m robot_platform.tools.platform_cli.main build sitl`
2. All Python tests pass: `python3 -m unittest robot_platform.tools.platform_cli.tests.test_main -v`
3. `grep -r 'app_params.h' robot_platform/runtime/control/` returns zero matches
4. `grep '\\\\n' robot_platform/runtime/bsp/sitl/main_sitl.c` returns zero matches
5. `grep 'ALL_HOST_TEST_TARGETS' robot_platform/tools/platform_cli/main.py` shows the widened constant
</verification>

<success_criteria>
- validate host_tests stage targets all 11 CTest targets (Phase 1 + 2 + 3 + 4)
- control_task_registry.c uses control_task_params.h with CONTROL_-prefixed macros, no app_params.h reference
- main_sitl.c printf calls produce real newlines
- All existing and new Python tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-v1-tech-debt-cleanup/06-01-SUMMARY.md`
</output>
